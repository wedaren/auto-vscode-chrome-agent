{
  "tasks": [
    {
      "id": "task_000",
      "type": "pm_research",
      "title": "技术可行性调研 + 项目骨架决策",
      "status": "done",
      "depends_on": [],
      "acceptance": "knowledge/ 下有四份研究文档，decisions.md 有完整技术决策",
      "acceptance_cmd": "test -f .agent/knowledge/vscode-lm.md && test -f .agent/knowledge/chrome-ext.md && test -f .agent/knowledge/websocket.md && test -f .agent/knowledge/chrome-devtools-mcp.md && test -s .agent/decisions.md && echo PASS || echo 'FAIL: knowledge 文档不完整'"
    },
    {
      "id": "task_001",
      "type": "coding",
      "title": "初始化 monorepo 项目结构",
      "status": "done",
      "depends_on": [
        "task_000"
      ],
      "acceptance": "pnpm install 无报错，packages/vscode-ext 和 packages/chrome-ext 目录存在且有 package.json",
      "acceptance_cmd": "test -f package.json && test -f packages/vscode-ext/package.json && test -f packages/chrome-ext/package.json && echo PASS || echo 'FAIL: 项目结构不完整'"
    },
    {
      "id": "task_002",
      "type": "coding",
      "title": "VSCode 插件骨架 + vscode.lm 基础调用",
      "status": "done",
      "depends_on": [
        "task_001"
      ],
      "acceptance": "插件编译通过，有 activate() 函数，有调用 vscode.lm 的代码",
      "acceptance_cmd": "cd packages/vscode-ext && npm run compile 2>&1 | grep -c 'error' | xargs -I{} test {} -eq 0 && grep -r 'vscode.lm' src/ && echo PASS || echo 'FAIL: 编译失败或未使用 vscode.lm'"
    },
    {
      "id": "task_003",
      "type": "coding",
      "title": "WebSocket server（VSCode 插件侧）",
      "status": "done",
      "depends_on": [
        "task_002"
      ],
      "acceptance": "ws server 代码存在，端口 7777，有连接/断开事件处理",
      "acceptance_cmd": "cd packages/vscode-ext && grep -r 'WebSocketServer\\|new Server' src/ > /dev/null && grep -r '7777' src/ > /dev/null && npm run compile 2>&1 | grep -c 'error' | xargs -I{} test {} -eq 0 && echo PASS || echo 'FAIL: WebSocket server 代码缺失或编译失败'"
    },
    {
      "id": "task_004",
      "type": "coding",
      "title": "Chrome 插件骨架（WXT）+ side panel UI",
      "status": "done",
      "depends_on": [
        "task_001"
      ],
      "acceptance": "WXT 结构正确，side panel 入口存在，有对话输入框组件",
      "acceptance_cmd": "cd packages/chrome-ext && test -f wxt.config.ts && npm run build 2>&1 | grep -i 'error' | grep -v 'no error' | wc -l | xargs -I{} test {} -eq 0 && echo PASS || echo 'FAIL: WXT 构建失败'"
    },
    {
      "id": "task_005",
      "type": "coding",
      "title": "Chrome ↔ VSCode WebSocket 消息收发",
      "status": "done",
      "depends_on": [
        "task_003",
        "task_004"
      ],
      "acceptance": "Chrome 插件有 WebSocket 客户端，能连接 localhost:7890",
      "acceptance_cmd": "cd packages/chrome-ext && grep -r 'WebSocket\\|ws://' src/ entrypoints/ > /dev/null && grep -r '7777' src/ entrypoints/ > /dev/null && npm run build 2>&1 | grep -i 'error' | wc -l | xargs -I{} test {} -eq 0 && echo PASS || echo 'FAIL: WebSocket 客户端代码缺失'"
    },
    {
      "id": "task_006",
      "type": "coding",
      "title": "页面上下文感知（URL / 标题 / 选中文本）",
      "status": "done",
      "depends_on": [
        "task_005"
      ],
      "acceptance": "content script 存在，能获取 URL、标题、选中文本",
      "acceptance_cmd": "cd packages/chrome-ext && grep -r 'document.title\\|location.href\\|getSelection' entrypoints/ src/ > /dev/null && npm run build 2>&1 | grep -i 'error' | wc -l | xargs -I{} test {} -eq 0 && echo PASS || echo 'FAIL: 上下文采集代码缺失'"
    },
    {
      "id": "task_007",
      "type": "coding",
      "title": "chrome-devtools-mcp 集成",
      "status": "done",
      "depends_on": [
        "task_006"
      ],
      "acceptance": "VSCode 插件有启动和连接 chrome-devtools-mcp 的代码",
      "acceptance_cmd": "cd packages/vscode-ext && grep -r 'chrome-devtools-mcp\\|devtools\\|McpClient' src/ > /dev/null && npm run compile 2>&1 | grep -c 'error' | xargs -I{} test {} -eq 0 && echo PASS || echo 'FAIL: MCP 集成代码缺失'"
    },
    {
      "id": "task_008",
      "type": "coding",
      "title": "深度报告生成",
      "status": "done",
      "depends_on": [
        "task_007"
      ],
      "acceptance": "有报告生成模块，能多页面探索并输出 Markdown",
      "acceptance_cmd": "cd packages/vscode-ext && find src/ -name '*report*' -o -name '*reporter*' | grep -q . && npm run compile 2>&1 | grep -c 'error' | xargs -I{} test {} -eq 0 && echo PASS || echo 'FAIL: 报告生成模块缺失'"
    },
    {
      "id": "task_009",
      "type": "validate",
      "title": "MVP 全量验收",
      "status": "done",
      "depends_on": [
        "task_008"
      ],
      "acceptance": "两个包均构建通过，program.md 停止条件全部满足",
      "acceptance_cmd": "cd packages/vscode-ext && npm run compile 2>&1 | grep -c 'error' | xargs -I{} test {} -eq 0 && cd ../chrome-ext && npm run build 2>&1 | grep -i 'error' | wc -l | xargs -I{} test {} -eq 0 && echo PASS || echo 'FAIL: 最终构建失败'"
    },
    {
      "id": "evo_v1_001",
      "type": "coding",
      "title": "LmService 新增 listModels() 和 selectModelById() 方法",
      "status": "done",
      "depends_on": [
        "task_009"
      ],
      "acceptance": "lm-service.ts 有 listModels() 返回所有可用模型信息数组（id/name/vendor/family/maxInputTokens），有 selectModelById(id) 可指定模型，编译通过",
      "acceptance_cmd": "cd packages/vscode-ext && grep -q 'listModels' src/lm-service.ts && grep -q 'selectModelById' src/lm-service.ts && npm run compile 2>&1 | grep -c 'error' | xargs -I{} test {} -eq 0 && echo PASS || echo 'FAIL: listModels/selectModelById 缺失或编译失败'",
      "evolution_goal": "chrome 插件对话能选 vscode.lm 模型对话"
    },
    {
      "id": "evo_v1_002",
      "type": "coding",
      "title": "WebSocket 新增 list_models / select_model 消息处理",
      "status": "done",
      "depends_on": [
        "evo_v1_001"
      ],
      "acceptance": "extension.ts 中注册了 list_models 和 select_model 的 WebSocket 消息处理器，收到 list_models 返回 models_list，收到 select_model 调用 LmService.selectModelById()，编译通过",
      "acceptance_cmd": "cd packages/vscode-ext && grep -q 'list_models' src/extension.ts && grep -q 'select_model' src/extension.ts && grep -q 'models_list' src/extension.ts && npm run compile 2>&1 | grep -c 'error' | xargs -I{} test {} -eq 0 && echo PASS || echo 'FAIL: WebSocket 模型消息处理缺失或编译失败'",
      "evolution_goal": "chrome 插件对话能选 vscode.lm 模型对话"
    },
    {
      "id": "evo_v1_003",
      "type": "coding",
      "title": "Chrome 侧新增 ModelSelector 下拉组件",
      "status": "done",
      "depends_on": [
        "evo_v1_002"
      ],
      "acceptance": "components/ModelSelector.tsx 存在，是一个 React 下拉选择组件，接受 models 列表和 onSelect 回调，构建通过",
      "acceptance_cmd": "cd packages/chrome-ext && test -f components/ModelSelector.tsx && grep -q 'ModelSelector' components/ModelSelector.tsx && npm run build 2>&1 | grep -i 'error' | wc -l | xargs -I{} test {} -eq 0 && echo PASS || echo 'FAIL: ModelSelector 组件缺失或构建失败'",
      "evolution_goal": "chrome 插件对话能选 vscode.lm 模型对话"
    },
    {
      "id": "evo_v1_004",
      "type": "coding",
      "title": "App.tsx 集成 ModelSelector，WebSocket 联动模型选择",
      "status": "done",
      "depends_on": [
        "evo_v1_003"
      ],
      "acceptance": "App.tsx 引入 ModelSelector，连接时自动请求 list_models，选择模型时发送 select_model，接收 models_list 更新下拉列表，构建通过",
      "acceptance_cmd": "cd packages/chrome-ext && grep -q 'ModelSelector' entrypoints/sidepanel/App.tsx && grep -q 'list_models' entrypoints/sidepanel/App.tsx && grep -q 'select_model' entrypoints/sidepanel/App.tsx && npm run build 2>&1 | grep -i 'error' | wc -l | xargs -I{} test {} -eq 0 && echo PASS || echo 'FAIL: App.tsx 模型选择集成缺失或构建失败'",
      "evolution_goal": "chrome 插件对话能选 vscode.lm 模型对话"
    },
    {
      "id": "evo_v1_005",
      "type": "validate",
      "title": "模型选择功能全量验收：双端构建通过 + 消息链路完整",
      "status": "done",
      "depends_on": [
        "evo_v1_004"
      ],
      "acceptance": "两个包均构建通过，Chrome 侧有 ModelSelector 组件和 list_models/select_model/models_list 消息处理，VSCode 侧有 listModels/selectModelById 和对应 WebSocket 处理",
      "acceptance_cmd": "cd packages/vscode-ext && grep -q 'listModels' src/lm-service.ts && grep -q 'list_models' src/extension.ts && grep -q 'select_model' src/extension.ts && npm run compile 2>&1 | grep -c 'error' | xargs -I{} test {} -eq 0 && cd ../chrome-ext && grep -q 'ModelSelector' entrypoints/sidepanel/App.tsx && grep -q 'models_list' entrypoints/sidepanel/App.tsx && npm run build 2>&1 | grep -i 'error' | wc -l | xargs -I{} test {} -eq 0 && echo PASS || echo 'FAIL: 模型选择功能验收失败'",
      "evolution_goal": "chrome 插件对话能选 vscode.lm 模型对话"
    },
    {
      "id": "evo_v1_006",
      "type": "coding",
      "title": "VSCode 侧流式响应：extension.ts chat 改用 sendMessageStreaming + 发送 chunk 消息",
      "status": "done",
      "depends_on": [
        "evo_v1_004"
      ],
      "acceptance": "extension.ts 中 chat 消息处理改用 lmService.sendMessageStreaming()，每个 fragment 通过 WebSocket 发送 chat_response_chunk（payload 含 text 和 done:false），完成时发送 chat_response_end（payload 含 fullText），支持 cancel_chat 消息中断生成（AbortController），编译通过",
      "acceptance_cmd": "cd packages/vscode-ext && grep -q 'sendMessageStreaming' src/extension.ts && grep -q 'chat_response_chunk' src/extension.ts && grep -q 'chat_response_end' src/extension.ts && grep -q 'cancel_chat' src/extension.ts && npm run compile 2>&1 | grep -c 'error' | xargs -I{} test {} -eq 0 && echo PASS || echo 'FAIL: 流式响应或取消机制缺失或编译失败'",
      "evolution_goal": "chrome 对话要做到 vscode Copilot 一样好用"
    },
    {
      "id": "evo_v1_007",
      "type": "coding",
      "title": "Chrome 侧流式响应：App.tsx 处理 chunk 增量渲染 + 停止生成按钮",
      "status": "done",
      "depends_on": [
        "evo_v1_006"
      ],
      "acceptance": "App.tsx 处理 chat_response_chunk 消息，增量追加到当前 assistant message 的 content；处理 chat_response_end 标记消息完成；等待响应时显示 isStreaming 状态；停止按钮点击发送 cancel_chat 消息；构建通过",
      "acceptance_cmd": "cd packages/chrome-ext && grep -q 'chat_response_chunk' entrypoints/sidepanel/App.tsx && grep -q 'chat_response_end' entrypoints/sidepanel/App.tsx && grep -q 'cancel_chat' entrypoints/sidepanel/App.tsx && grep -q 'isStreaming' entrypoints/sidepanel/App.tsx && npm run build 2>&1 | grep -i 'error' | wc -l | xargs -I{} test {} -eq 0 && echo PASS || echo 'FAIL: 流式响应处理或停止生成缺失或构建失败'",
      "evolution_goal": "chrome 对话要做到 vscode Copilot 一样好用"
    },
    {
      "id": "evo_v1_008",
      "type": "coding",
      "title": "MessageBubble 组件：Markdown 渲染 + 代码语法高亮 + 代码块复制按钮",
      "status": "done",
      "depends_on": [
        "evo_v1_007"
      ],
      "acceptance": "新建 components/MessageBubble.tsx 组件；安装 marked 和 highlight.js 依赖；assistant 消息渲染为 Markdown HTML（标题/列表/粗体/链接/代码块）；代码块有语法高亮；每个代码块有复制按钮；user 消息保持纯文本样式；App.tsx 中消息列表使用 MessageBubble 替代原有内联渲染；构建通过",
      "acceptance_cmd": "cd packages/chrome-ext && test -f components/MessageBubble.tsx && grep -q 'marked' components/MessageBubble.tsx && grep -q 'highlight' components/MessageBubble.tsx && grep -q 'MessageBubble' entrypoints/sidepanel/App.tsx && grep -q 'marked' package.json && npm run build 2>&1 | grep -i 'error' | wc -l | xargs -I{} test {} -eq 0 && echo PASS || echo 'FAIL: MessageBubble 组件或 Markdown 渲染缺失或构建失败'",
      "evolution_goal": "chrome 对话要做到 vscode Copilot 一样好用"
    },
    {
      "id": "evo_v1_009",
      "type": "coding",
      "title": "TypingIndicator 组件：等待/流式响应时显示思考中动画",
      "status": "done",
      "depends_on": [
        "evo_v1_008"
      ],
      "acceptance": "新建 components/TypingIndicator.tsx，三点跳动 CSS 动画；App.tsx 在 isStreaming 状态且 assistant 消息 content 为空时显示 TypingIndicator；流式内容开始到达后隐藏指示器显示实际内容；构建通过",
      "acceptance_cmd": "cd packages/chrome-ext && test -f components/TypingIndicator.tsx && grep -q 'TypingIndicator' entrypoints/sidepanel/App.tsx && grep -q 'isStreaming' entrypoints/sidepanel/App.tsx && npm run build 2>&1 | grep -i 'error' | wc -l | xargs -I{} test {} -eq 0 && echo PASS || echo 'FAIL: TypingIndicator 缺失或构建失败'",
      "evolution_goal": "chrome 对话要做到 vscode Copilot 一样好用"
    },
    {
      "id": "evo_v1_010",
      "type": "validate",
      "title": "Copilot 级体验全量验收：流式 + Markdown + 指示器 + 停止，双端构建通过",
      "status": "done",
      "depends_on": [
        "evo_v1_009"
      ],
      "acceptance": "双端构建通过；VSCode 侧有 sendMessageStreaming 流式调用 + chat_response_chunk/end 消息 + cancel_chat 取消；Chrome 侧有流式消息处理 + MessageBubble Markdown 渲染 + TypingIndicator + 停止生成按钮",
      "acceptance_cmd": "cd packages/vscode-ext && grep -q 'sendMessageStreaming' src/extension.ts && grep -q 'chat_response_chunk' src/extension.ts && grep -q 'cancel_chat' src/extension.ts && npm run compile 2>&1 | grep -c 'error' | xargs -I{} test {} -eq 0 && cd ../chrome-ext && grep -q 'chat_response_chunk' entrypoints/sidepanel/App.tsx && test -f components/MessageBubble.tsx && test -f components/TypingIndicator.tsx && grep -q 'cancel_chat' entrypoints/sidepanel/App.tsx && npm run build 2>&1 | grep -i 'error' | wc -l | xargs -I{} test {} -eq 0 && echo PASS || echo 'FAIL: Copilot 级体验验收失败'",
      "evolution_goal": "chrome 对话要做到 vscode Copilot 一样好用"
    },
    {
      "id": "evo_v1_011",
      "type": "coding",
      "title": "修复 models_list 和 select_model 消息格式不匹配（P0 致命 bug）",
      "status": "done",
      "depends_on": [
        "evo_v1_010"
      ],
      "acceptance": "VSCode extension.ts 中 list_models 处理器返回 payload: { models } 格式（对象包裹数组）；select_model 处理器从 payload 提取 modelId 字段（与 Chrome 侧一致）；Chrome App.tsx models_list 处理正确解析 payload.models；select_model 发送 payload.modelId；双端编译/构建通过",
      "acceptance_cmd": "cd packages/vscode-ext && grep -q '{ models }\\|{models}\\|{ models:' src/extension.ts && grep -q 'modelId' src/extension.ts && npm run compile 2>&1 | grep -c 'error' | xargs -I{} test {} -eq 0 && cd ../chrome-ext && npm run build 2>&1 | grep -i 'error' | wc -l | xargs -I{} test {} -eq 0 && echo PASS || echo 'FAIL: 消息格式修复失败或构建失败'",
      "evolution_goal": "现在 chrome 对话无法正常对话"
    },
    {
      "id": "evo_v1_012",
      "type": "coding",
      "title": "修复 chat 消息处理：提取 context 并拼入 LM 调用的 system prompt",
      "status": "done",
      "depends_on": [
        "evo_v1_011"
      ],
      "acceptance": "extension.ts chat 处理器从 msg.payload 提取 context（url/title/selectedText）；将上下文信息动态拼入 system prompt（如有 url 则提示'用户正在浏览 {url}'，有 selectedText 则附加选中文本）；传入 lmService.sendMessageStreaming()；编译通过",
      "acceptance_cmd": "cd packages/vscode-ext && grep -q 'context' src/extension.ts && grep -q 'url\\|title\\|selectedText' src/extension.ts && npm run compile 2>&1 | grep -c 'error' | xargs -I{} test {} -eq 0 && echo PASS || echo 'FAIL: context 处理缺失或编译失败'",
      "evolution_goal": "现在 chrome 对话无法正常对话"
    },
    {
      "id": "evo_v1_013",
      "type": "coding",
      "title": "修复 isStreaming 状态管理：发送失败恢复 + chat_response_end 边缘情况",
      "status": "done",
      "depends_on": [
        "evo_v1_012"
      ],
      "acceptance": "App.tsx handleSendMessage 检查 sendMessage 返回值，发送失败时恢复 isStreaming=false 并移除空 assistant 消息或显示错误提示；chat_response_end 处理改用 typeof fullText === 'string' 判断（兼容空字符串场景）；WebSocket 断连时自动恢复 isStreaming 状态；构建通过",
      "acceptance_cmd": "cd packages/chrome-ext && grep -q 'sendMessage.*chat' entrypoints/sidepanel/App.tsx && grep -q 'setIsStreaming(false)' entrypoints/sidepanel/App.tsx && npm run build 2>&1 | grep -i 'error' | wc -l | xargs -I{} test {} -eq 0 && echo PASS || echo 'FAIL: 状态管理修复缺失或构建失败'",
      "evolution_goal": "现在 chrome 对话无法正常对话"
    },
    {
      "id": "evo_v1_014",
      "type": "validate",
      "title": "Chrome 对话修复全量验收：消息格式 + 上下文感知 + 状态管理，双端构建通过",
      "status": "done",
      "depends_on": [
        "evo_v1_013"
      ],
      "acceptance": "双端构建通过；VSCode 侧 models_list 返回 { models } 格式 + select_model 接收 modelId + chat 提取 context 拼入 prompt；Chrome 侧 models_list 正确解析 + isStreaming 失败恢复 + chat_response_end 兼容空字符串",
      "acceptance_cmd": "cd packages/vscode-ext && grep -q 'modelId' src/extension.ts && grep -q 'context' src/extension.ts && npm run compile 2>&1 | grep -c 'error' | xargs -I{} test {} -eq 0 && cd ../chrome-ext && npm run build 2>&1 | grep -i 'error' | wc -l | xargs -I{} test {} -eq 0 && echo PASS || echo 'FAIL: Chrome 对话修复验收失败'",
      "evolution_goal": "现在 chrome 对话无法正常对话"
    },
    {
      "id": "evo_v2_001",
      "type": "coding",
      "title": "VSCode 重构：从 extension.ts 抽取 MessageHandler 消息路由类",
      "status": "done",
      "depends_on": [
        "evo_v1_014"
      ],
      "acceptance": "新建 src/message-handler.ts，包含 MessageHandler 类，封装所有 WebSocket 消息处理逻辑（list_models/select_model/chat/cancel_chat）、浏览器上下文→system prompt 构建、CancellationToken 生命周期管理；extension.ts 中消息路由部分替换为调用 MessageHandler；编译通过",
      "acceptance_cmd": "cd packages/vscode-ext && test -f src/message-handler.ts && grep -q 'class MessageHandler\\|export class MessageHandler' src/message-handler.ts && grep -q 'list_models\\|handleListModels' src/message-handler.ts && grep -q 'chat_response_chunk\\|handleChat' src/message-handler.ts && grep -q 'cancel_chat\\|handleCancelChat' src/message-handler.ts && grep -q 'MessageHandler' src/extension.ts && npm run compile 2>&1 | grep -c 'error' | xargs -I{} test {} -eq 0 && echo PASS || echo 'FAIL: MessageHandler 抽取失败或编译失败'",
      "evolution_goal": "重构这个项目"
    },
    {
      "id": "evo_v2_002",
      "type": "coding",
      "title": "VSCode 重构：从 extension.ts 抽取 CommandRegistry 命令注册模块",
      "status": "done",
      "depends_on": [
        "evo_v2_001"
      ],
      "acceptance": "新建 src/command-registry.ts，封装所有 vscode.commands.registerCommand 调用（generateReport/connectDevtools/ask）；extension.ts 中命令注册部分替换为调用 CommandRegistry；extension.ts 仅保留 activate/deactivate 编排逻辑（< 100 行）；编译通过",
      "acceptance_cmd": "cd packages/vscode-ext && test -f src/command-registry.ts && grep -q 'generateReport\\|connectDevtools\\|ask' src/command-registry.ts && wc -l < src/extension.ts | xargs -I{} test {} -lt 120 && npm run compile 2>&1 | grep -c 'error' | xargs -I{} test {} -eq 0 && echo PASS || echo 'FAIL: CommandRegistry 抽取失败、extension.ts 未缩减或编译失败'",
      "evolution_goal": "重构这个项目"
    },
    {
      "id": "evo_v2_003",
      "type": "coding",
      "title": "Chrome 重构：从 App.tsx 抽取 useChat + useWebSocket 自定义 Hooks",
      "status": "done",
      "depends_on": [
        "evo_v1_014"
      ],
      "acceptance": "新建 hooks/useChat.ts（封装 messages 状态、isStreaming、streamingMsgIdRef、handleSendMessage、handleCancel 逻辑）；新建 hooks/useWebSocket.ts（封装 WsClient 初始化、连接状态管理、消息分发、断连恢复）；App.tsx 中对应逻辑替换为调用这两个 Hook；构建通过",
      "acceptance_cmd": "cd packages/chrome-ext && test -f hooks/useChat.ts && test -f hooks/useWebSocket.ts && grep -q 'useChat' entrypoints/sidepanel/App.tsx && grep -q 'useWebSocket' entrypoints/sidepanel/App.tsx && grep -q 'isStreaming\\|handleSend\\|messages' hooks/useChat.ts && grep -q 'WsClient\\|onMessage\\|connectionState' hooks/useWebSocket.ts && npm run build 2>&1 | grep -i 'error' | wc -l | xargs -I{} test {} -eq 0 && echo PASS || echo 'FAIL: useChat/useWebSocket Hook 抽取失败或构建失败'",
      "evolution_goal": "重构这个项目"
    },
    {
      "id": "evo_v2_004",
      "type": "coding",
      "title": "Chrome 重构：抽取 usePageContext Hook + createMessage 工具函数，App.tsx 精简为纯 UI",
      "status": "done",
      "depends_on": [
        "evo_v2_003"
      ],
      "acceptance": "新建 hooks/usePageContext.ts（封装 fetchPageContext、browser.runtime.onMessage 监听、pageContext 状态）；新建 utils/message-factory.ts（createMessage 工厂函数，消除重复的消息创建代码）；App.tsx 仅保留 UI 渲染和 Hook 调用（< 150 行）；构建通过",
      "acceptance_cmd": "cd packages/chrome-ext && test -f hooks/usePageContext.ts && test -f utils/message-factory.ts && grep -q 'usePageContext' entrypoints/sidepanel/App.tsx && grep -q 'createMessage' utils/message-factory.ts && wc -l < entrypoints/sidepanel/App.tsx | xargs -I{} test {} -lt 180 && npm run build 2>&1 | grep -i 'error' | wc -l | xargs -I{} test {} -eq 0 && echo PASS || echo 'FAIL: usePageContext/message-factory 抽取失败、App.tsx 未缩减或构建失败'",
      "evolution_goal": "重构这个项目"
    },
    {
      "id": "evo_v2_005",
      "type": "validate",
      "title": "重构全量验收：双端构建通过 + 核心文件缩减 + 新模块完整",
      "status": "done",
      "depends_on": [
        "evo_v2_002",
        "evo_v2_004"
      ],
      "acceptance": "双端构建通过；VSCode 侧 extension.ts < 100 行，新增 message-handler.ts 和 command-registry.ts；Chrome 侧 App.tsx < 150 行，新增 hooks/useChat.ts、hooks/useWebSocket.ts、hooks/usePageContext.ts、utils/message-factory.ts；功能行为不变",
      "acceptance_cmd": "cd packages/vscode-ext && test -f src/message-handler.ts && test -f src/command-registry.ts && npm run compile 2>&1 | grep -c 'error' | xargs -I{} test {} -eq 0 && cd ../chrome-ext && test -f hooks/useChat.ts && test -f hooks/useWebSocket.ts && test -f hooks/usePageContext.ts && test -f utils/message-factory.ts && npm run build 2>&1 | grep -i 'error' | wc -l | xargs -I{} test {} -eq 0 && echo PASS || echo 'FAIL: 重构验收失败'",
      "evolution_goal": "重构这个项目"
    },
    {
      "id": "evo_v3_001",
      "type": "coding",
      "title": "VSCode: 创建 AgentLoop 类，实现 ReAct 风格的 think→act→observe 循环",
      "status": "done",
      "depends_on": [
        "evo_v2_005"
      ],
      "acceptance": "新建 src/agent-loop.ts，包含 AgentLoop 类；构造函数接收 LmService、McpClient、OutputChannel；run(userMessage, context, onStep, token) 方法实现 ReAct 循环：1) 构建含工具描述的 system prompt 2) LM 输出包含 ACTION: toolName(args) 格式的思考 3) 解析 ACTION 调用 McpClient.callTool() 4) 将结果作为 OBSERVATION 反馈给 LM 5) 循环直到 LM 输出 FINAL_ANSWER 或达到 maxSteps(默认5)；onStep 回调通知每步状态(type: think/act/observe, content)；支持 CancellationToken 中断；编译通过",
      "acceptance_cmd": "cd packages/vscode-ext && test -f src/agent-loop.ts && grep -q 'class AgentLoop' src/agent-loop.ts && grep -q 'McpClient' src/agent-loop.ts && grep -q 'LmService' src/agent-loop.ts && grep -q 'FINAL_ANSWER\\|ACTION' src/agent-loop.ts && grep -q 'onStep\\|AgentStep' src/agent-loop.ts && grep -q 'maxSteps\\|MAX_STEPS' src/agent-loop.ts && npm run compile 2>&1 | grep -c 'error' | xargs -I{} test {} -eq 0 && echo PASS || echo 'FAIL: AgentLoop 类缺失或编译失败'",
      "evolution_goal": "Agent 愿景：Agentic Tool Use — 让 LM 在对话中自主使用 MCP 浏览器工具"
    },
    {
      "id": "evo_v3_002",
      "type": "coding",
      "title": "VSCode: MessageHandler 集成 AgentLoop，新增 agent_step/agent_complete 消息类型",
      "status": "done",
      "depends_on": [
        "evo_v3_001"
      ],
      "acceptance": "message-handler.ts 接收 McpClient 依赖注入；handleChat 判断：若 McpClient 已连接，使用 AgentLoop.run() 替代直接 LM 调用；每个 AgentStep 通过 WebSocket 发送 agent_step 消息（payload: { step, type, content, toolName?, toolArgs? }）；循环完成发送 agent_complete 消息（payload: { finalAnswer, steps, totalSteps }）；若 McpClient 未连接则保持原有直接 LM 流式对话行为；编译通过",
      "acceptance_cmd": "cd packages/vscode-ext && grep -q 'AgentLoop\\|agentLoop' src/message-handler.ts && grep -q 'McpClient\\|mcpClient' src/message-handler.ts && grep -q 'agent_step' src/message-handler.ts && grep -q 'agent_complete' src/message-handler.ts && npm run compile 2>&1 | grep -c 'error' | xargs -I{} test {} -eq 0 && echo PASS || echo 'FAIL: MessageHandler AgentLoop 集成缺失或编译失败'",
      "evolution_goal": "Agent 愿景：Agentic Tool Use — 让 LM 在对话中自主使用 MCP 浏览器工具"
    },
    {
      "id": "evo_v3_003",
      "type": "coding",
      "title": "Chrome: 创建 AgentStepView 组件，展示 Agent 思考和工具调用过程",
      "status": "done",
      "depends_on": [
        "evo_v3_002"
      ],
      "acceptance": "新建 components/AgentStepView.tsx；接收 steps 数组（{step, type, content, toolName?, toolArgs?}）；think 步骤显示为灰色斜体带🧠图标；act 步骤显示为蓝色带⚡图标和工具名称徽章；observe 步骤显示为绿色带📋图标，内容可折叠（默认折叠超过 200 字的观察结果）；整体可折叠/展开（默认展开最近3步）；有 isRunning prop 控制最后一步的加载动画；构建通过",
      "acceptance_cmd": "cd packages/chrome-ext && test -f components/AgentStepView.tsx && grep -q 'AgentStepView' components/AgentStepView.tsx && grep -q 'think\\|act\\|observe' components/AgentStepView.tsx && grep -q 'isRunning\\|step' components/AgentStepView.tsx && npm run build 2>&1 | grep -i 'error' | wc -l | xargs -I{} test {} -eq 0 && echo PASS || echo 'FAIL: AgentStepView 组件缺失或构建失败'",
      "evolution_goal": "Agent 愿景：Agentic Tool Use — 让 LM 在对话中自主使用 MCP 浏览器工具"
    },
    {
      "id": "evo_v3_004",
      "type": "coding",
      "title": "Chrome: useChat 处理 agent_step/agent_complete 消息，MessageBubble 集成 AgentStepView",
      "status": "done",
      "depends_on": [
        "evo_v3_003"
      ],
      "acceptance": "hooks/useChat.ts 中 Message 类型新增可选 steps 数组和 isAgentMode 字段；处理 agent_step 消息：追加步骤到当前 assistant message 的 steps 数组，isStreaming 保持 true；处理 agent_complete 消息：设置 finalAnswer 为 content，isStreaming=false；components/MessageBubble.tsx 当 message.steps 存在时渲染 AgentStepView 在正文上方；App.tsx 无需大改（数据通过 useChat 流入）；构建通过",
      "acceptance_cmd": "cd packages/chrome-ext && grep -q 'agent_step' hooks/useChat.ts && grep -q 'agent_complete' hooks/useChat.ts && grep -q 'steps\\|AgentStep' hooks/useChat.ts && grep -q 'AgentStepView' components/MessageBubble.tsx && npm run build 2>&1 | grep -i 'error' | wc -l | xargs -I{} test {} -eq 0 && echo PASS || echo 'FAIL: agent 消息处理或 UI 集成缺失或构建失败'",
      "evolution_goal": "Agent 愿景：Agentic Tool Use — 让 LM 在对话中自主使用 MCP 浏览器工具"
    },
    {
      "id": "evo_v3_005",
      "type": "validate",
      "title": "Agentic Tool Use 全量验收：Agent Loop + 消息协议 + UI 展示，双端构建通过",
      "status": "done",
      "depends_on": [
        "evo_v3_004"
      ],
      "acceptance": "双端构建通过；VSCode 侧有 AgentLoop 类（ReAct 循环 + MCP 工具调用 + 步数限制 + 取消支持）、MessageHandler 集成 AgentLoop 并发送 agent_step/agent_complete 消息；Chrome 侧 useChat 处理 agent_step/agent_complete、AgentStepView 组件展示步骤、MessageBubble 集成 AgentStepView",
      "acceptance_cmd": "cd packages/vscode-ext && test -f src/agent-loop.ts && grep -q 'class AgentLoop' src/agent-loop.ts && grep -q 'agent_step' src/message-handler.ts && grep -q 'agent_complete' src/message-handler.ts && npm run compile 2>&1 | grep -c 'error' | xargs -I{} test {} -eq 0 && cd ../chrome-ext && test -f components/AgentStepView.tsx && grep -q 'agent_step' hooks/useChat.ts && grep -q 'agent_complete' hooks/useChat.ts && grep -q 'AgentStepView' components/MessageBubble.tsx && npm run build 2>&1 | grep -i 'error' | wc -l | xargs -I{} test {} -eq 0 && echo PASS || echo 'FAIL: Agentic Tool Use 验收失败'",
      "evolution_goal": "Agent 愿景：Agentic Tool Use — 让 LM 在对话中自主使用 MCP 浏览器工具"
    },
    {
      "id": "evo_v4_001",
      "type": "coding",
      "title": "Chrome 侧聊天记录持久化：chrome.storage.local 存储会话数据",
      "status": "done",
      "depends_on": [
        "evo_v3_005"
      ],
      "acceptance": "新建 hooks/useChatStorage.ts，定义 Conversation 数据模型（id/title/messages[]/createdAt/updatedAt）；使用 chrome.storage.local 实现 saveConversation/loadConversation/listConversations/deleteConversation；useChat.ts 集成 useChatStorage，消息变更时自动持久化；页面刷新后消息不丢失；会话标题自动取首条用户消息前20字；构建通过",
      "acceptance_cmd": "cd packages/chrome-ext && test -f hooks/useChatStorage.ts && grep -q 'chrome.storage.local\\|storage.local\\|browser.storage.local' hooks/useChatStorage.ts && grep -q 'Conversation\\|conversation' hooks/useChatStorage.ts && grep -q 'saveConversation\\|save' hooks/useChatStorage.ts && grep -q 'loadConversation\\|load' hooks/useChatStorage.ts && npm run build 2>&1 | grep -i 'error' | wc -l | xargs -I{} test {} -eq 0 && echo PASS || echo 'FAIL: 聊天记录持久化缺失或构建失败'",
      "evolution_goal": "不考虑时间，使用最优解，优化聊天体验"
    },
    {
      "id": "evo_v4_002",
      "type": "coding",
      "title": "Chrome 侧多会话管理：会话列表侧栏 + 新建/切换/删除会话",
      "status": "done",
      "depends_on": [
        "evo_v4_001"
      ],
      "acceptance": "新建 components/ConversationList.tsx 侧栏组件，展示所有会话（标题 + 时间 + 消息数预览）；支持新建会话（按钮或快捷操作）、点击切换会话（加载对应历史消息）、滑动或按钮删除会话；App.tsx 集成 ConversationList，左侧或顶部抽屉式布局；当前活跃会话高亮；空会话列表显示引导文案；构建通过",
      "acceptance_cmd": "cd packages/chrome-ext && test -f components/ConversationList.tsx && grep -q 'ConversationList' components/ConversationList.tsx && grep -q 'ConversationList' entrypoints/sidepanel/App.tsx && grep -q 'delete\\|remove\\|onDelete' components/ConversationList.tsx && grep -q 'onClick\\|onSelect\\|onSwitch' components/ConversationList.tsx && npm run build 2>&1 | grep -i 'error' | wc -l | xargs -I{} test {} -eq 0 && echo PASS || echo 'FAIL: 多会话管理组件缺失或构建失败'",
      "evolution_goal": "不考虑时间，使用最优解，优化聊天体验"
    },
    {
      "id": "evo_v4_003",
      "type": "coding",
      "title": "Chrome 侧消息交互增强：复制消息/重新生成/时间戳显示/欢迎引导页",
      "status": "done",
      "depends_on": [
        "evo_v4_002"
      ],
      "acceptance": "MessageBubble.tsx 新增 hover 时显示操作栏：复制整条消息按钮（navigator.clipboard）、assistant 消息有重新生成按钮（重发上一条 user 消息）；每条消息显示相对时间戳（刚刚/N分钟前/N小时前）；新建 components/WelcomeScreen.tsx 空会话引导页，包含欢迎文案和 3-4 个预设 prompt 建议按钮（点击直接发送）；App.tsx 在消息为空时显示 WelcomeScreen；构建通过",
      "acceptance_cmd": "cd packages/chrome-ext && grep -q 'clipboard\\|navigator.clipboard\\|copyToClipboard' components/MessageBubble.tsx && grep -q 'regenerate\\|retry\\|onRegenerate' components/MessageBubble.tsx && test -f components/WelcomeScreen.tsx && grep -q 'WelcomeScreen' entrypoints/sidepanel/App.tsx && npm run build 2>&1 | grep -i 'error' | wc -l | xargs -I{} test {} -eq 0 && echo PASS || echo 'FAIL: 消息交互增强或欢迎页缺失或构建失败'",
      "evolution_goal": "不考虑时间，使用最优解，优化聊天体验"
    },
    {
      "id": "evo_v4_004",
      "type": "coding",
      "title": "Chrome 侧输入体验增强：斜杠命令(/new /clear) + 键盘快捷键 + 输入历史",
      "status": "done",
      "depends_on": [
        "evo_v4_003"
      ],
      "acceptance": "ChatInput.tsx 支持斜杠命令：输入 / 时弹出命令菜单面板，支持 /new（新建会话）、/clear（清空当前会话）、/models（切换模型）；键盘快捷键：Cmd/Ctrl+Shift+O 新建会话、Cmd/Ctrl+L 清空会话；上箭头键在输入框为空时填入上一条用户消息（方便重新编辑发送）；输入框最大高度提升到 200px，过渡动画平滑；构建通过",
      "acceptance_cmd": "cd packages/chrome-ext && grep -q '/new\\|/clear\\|slash\\|SlashCommand\\|commandMenu' components/ChatInput.tsx && grep -q 'ArrowUp\\|keydown\\|onKeyDown' components/ChatInput.tsx && npm run build 2>&1 | grep -i 'error' | wc -l | xargs -I{} test {} -eq 0 && echo PASS || echo 'FAIL: 输入体验增强缺失或构建失败'",
      "evolution_goal": "不考虑时间，使用最优解，优化聊天体验"
    },
    {
      "id": "evo_v4_005",
      "type": "validate",
      "title": "聊天体验优化全量验收：持久化 + 多会话 + 消息交互 + 输入增强，构建通过",
      "status": "done",
      "depends_on": [
        "evo_v4_004"
      ],
      "acceptance": "Chrome 构建通过；hooks/useChatStorage.ts 持久化层完整（CRUD）；ConversationList.tsx 多会话管理可用；MessageBubble 有复制/重新生成/时间戳；WelcomeScreen 欢迎页存在；ChatInput 支持斜杠命令和快捷键",
      "acceptance_cmd": "cd packages/chrome-ext && test -f hooks/useChatStorage.ts && test -f components/ConversationList.tsx && test -f components/WelcomeScreen.tsx && grep -q 'clipboard' components/MessageBubble.tsx && grep -qi 'regenerate\\|retry' components/MessageBubble.tsx && grep -q '/new\\|slash\\|SlashCommand' components/ChatInput.tsx && npm run build 2>&1 | grep -i 'error' | wc -l | xargs -I{} test {} -eq 0 && echo PASS || echo 'FAIL: 聊天体验优化验收失败'",
      "evolution_goal": "不考虑时间，使用最优解，优化聊天体验"
    },
    {
      "id": "evo_v5_001",
      "type": "coding",
      "title": "VSCode Activity Bar 视图容器注册 + 3 个 TreeView 占位声明",
      "status": "done",
      "depends_on": [
        "evo_v4_005"
      ],
      "acceptance": "package.json 新增 viewsContainers.activitybar 配置（含自定义图标），contributes.views 注册 3 个视图：browser-agent-connection（连接状态）、browser-agent-messages（消息日志）、browser-agent-agent-loop（Agent 循环）；src/ 下有 3 个空 TreeDataProvider 文件（connection-tree.ts、message-tree.ts、agent-tree.ts）并在 extension.ts 中注册；编译通过",
      "acceptance_cmd": "cd packages/vscode-ext && grep -q 'viewsContainers' package.json && grep -q 'browser-agent-connection' package.json && grep -q 'browser-agent-messages' package.json && grep -q 'browser-agent-agent-loop' package.json && test -f src/connection-tree.ts && test -f src/message-tree.ts && test -f src/agent-tree.ts && grep -q 'registerTreeDataProvider\\|createTreeView' src/extension.ts && npm run compile 2>&1 | grep -c 'error' | xargs -I{} test {} -eq 0 && echo PASS || echo 'FAIL: Activity Bar 视图注册不完整或编译失败'",
      "evolution_goal": "不考虑时间，使用最优解，方便调试体验，在 vsocde 插件有对应的视图方便查看与使用"
    },
    {
      "id": "evo_v5_002",
      "type": "coding",
      "title": "连接状态 TreeView：WebSocket 服务器状态 + MCP 连接状态 + 当前模型信息实时展示",
      "status": "done",
      "depends_on": [
        "evo_v5_001"
      ],
      "acceptance": "connection-tree.ts 实现 TreeDataProvider，展示 3 个顶级节点：① WebSocket Server（端口、监听状态、已连接客户端数）② MCP 连接（状态、已发现工具列表作为子节点）③ 当前模型（名称、vendor、family、maxInputTokens）；ws-server.ts 和 mcp-client.ts 暴露状态变更事件，TreeView 自动刷新；编译通过",
      "acceptance_cmd": "cd packages/vscode-ext && grep -q 'TreeDataProvider' src/connection-tree.ts && grep -q 'onDidChangeTreeData' src/connection-tree.ts && grep -q 'getChildren\\|getTreeItem' src/connection-tree.ts && npm run compile 2>&1 | grep -c 'error' | xargs -I{} test {} -eq 0 && echo PASS || echo 'FAIL: 连接状态 TreeView 实现不完整或编译失败'",
      "evolution_goal": "不考虑时间，使用最优解，方便调试体验，在 vsocde 插件有对应的视图方便查看与使用"
    },
    {
      "id": "evo_v5_003",
      "type": "coding",
      "title": "消息检查器 TreeView：实时 WebSocket 消息流日志 + 点击查看完整 JSON",
      "status": "done",
      "depends_on": [
        "evo_v5_002"
      ],
      "acceptance": "message-tree.ts 实现 TreeDataProvider，显示收发的 WebSocket 消息列表（最近 200 条环形缓冲）；每条显示方向箭头（↑发送/↓接收）+ type + 时间戳 + 截断 payload；点击消息节点在编辑器中打开完整 JSON（使用 TextDocumentContentProvider 虚拟文档）；ws-server.ts 的 send/broadcast/onMessage 处注入消息采集钩子；提供清空日志命令 browser-agent.clearMessageLog；编译通过",
      "acceptance_cmd": "cd packages/vscode-ext && grep -q 'TreeDataProvider' src/message-tree.ts && grep -q 'TextDocumentContentProvider\\|openTextDocument\\|showTextDocument' src/message-tree.ts && grep -q 'clearMessageLog' src/message-tree.ts && grep -q 'clearMessageLog' package.json && npm run compile 2>&1 | grep -c 'error' | xargs -I{} test {} -eq 0 && echo PASS || echo 'FAIL: 消息检查器 TreeView 实现不完整或编译失败'",
      "evolution_goal": "不考虑时间，使用最优解，方便调试体验，在 vsocde 插件有对应的视图方便查看与使用"
    },
    {
      "id": "evo_v5_004",
      "type": "coding",
      "title": "Agent 循环可视化 TreeView：ReAct 步骤实时展示 + 历史执行记录浏览",
      "status": "done",
      "depends_on": [
        "evo_v5_003"
      ],
      "acceptance": "agent-tree.ts 实现 TreeDataProvider，顶级节点为每次 Agent 运行（显示时间戳+状态:running/completed/cancelled/error）；子节点为各步骤（think🧠/act⚡/observe📋）含 step 序号、toolName、截断 content；运行中的循环实时追加步骤并自动刷新；agent-loop.ts 的 stepCallback 注入 TreeView 更新钩子；保留最近 20 次运行记录；编译通过",
      "acceptance_cmd": "cd packages/vscode-ext && grep -q 'TreeDataProvider' src/agent-tree.ts && grep -q 'onDidChangeTreeData' src/agent-tree.ts && grep -q 'think\\|act\\|observe' src/agent-tree.ts && npm run compile 2>&1 | grep -c 'error' | xargs -I{} test {} -eq 0 && echo PASS || echo 'FAIL: Agent 循环 TreeView 实现不完整或编译失败'",
      "evolution_goal": "不考虑时间，使用最优解，方便调试体验，在 vsocde 插件有对应的视图方便查看与使用"
    },
    {
      "id": "evo_v5_005",
      "type": "validate",
      "title": "调试视图全量验收：3 个 TreeView 功能完整 + 调试命令可用 + 构建通过",
      "status": "done",
      "depends_on": [
        "evo_v5_004"
      ],
      "acceptance": "VSCode 编译通过；package.json 包含完整的 viewsContainers + views + commands 声明；3 个 TreeDataProvider 均已注册且实现 getTreeItem/getChildren/onDidChangeTreeData；connection-tree 展示 WS/MCP/模型状态；message-tree 支持实时消息流和清空命令；agent-tree 支持多次运行记录和步骤展示；所有新增命令已注册（clearMessageLog, exportDebugLog）；Chrome 构建也通过（无回归）",
      "acceptance_cmd": "cd packages/vscode-ext && npm run compile 2>&1 | grep -c 'error' | xargs -I{} test {} -eq 0 && grep -q 'viewsContainers' package.json && grep -q 'browser-agent-connection' package.json && grep -q 'browser-agent-messages' package.json && grep -q 'browser-agent-agent-loop' package.json && grep -q 'TreeDataProvider' src/connection-tree.ts && grep -q 'TreeDataProvider' src/message-tree.ts && grep -q 'TreeDataProvider' src/agent-tree.ts && grep -q 'registerTreeDataProvider\\|createTreeView' src/extension.ts && cd ../chrome-ext && npm run build 2>&1 | grep -i 'error' | wc -l | xargs -I{} test {} -eq 0 && echo PASS || echo 'FAIL: 调试视图全量验收失败'",
      "evolution_goal": "不考虑时间，使用最优解，方便调试体验，在 vsocde 插件有对应的视图方便查看与使用"
    },
    {
      "id": "evo_v6_001",
      "type": "coding",
      "title": "Chrome 侧浏览器操作执行引擎：content script 增强支持 click/type/scroll/navigate/querySelector/getTextContent/screenshot 等 DOM 操作",
      "status": "done",
      "depends_on": [
        "evo_v5_005"
      ],
      "acceptance": "新建 packages/chrome-ext/utils/action-executor.ts 定义 BrowserAction 接口和所有支持的操作类型（click/type/scroll/navigate/querySelector/querySelectorAll/getTextContent/getAttribute/getValue/screenshot/waitForElement/highlight）；content.ts 增强：新增 EXECUTE_ACTION 消息监听器，接收 action 对象并执行对应 DOM 操作，返回结构化结果（success/data/error）；background.ts 增强：新增 EXECUTE_ACTION 消息路由，将来自 side panel 或 WebSocket 的操作请求转发到活动 tab 的 content script 并回传结果；click 操作支持 CSS selector 定位 + 可选的文本匹配过滤；type 操作支持 focus → 清空 → 逐字输入 → 触发 input/change 事件；scroll 操作支持 to-top/to-bottom/by-pixels/to-element 四种模式；screenshot 使用 chrome.tabs.captureVisibleTab API（background 中执行）返回 base64 dataURL；querySelector 返回匹配元素的 tagName/id/className/textContent/href/src 等属性；Chrome 构建通过",
      "acceptance_cmd": "cd packages/chrome-ext && test -f utils/action-executor.ts && grep -q 'BrowserAction\\|ActionType\\|executeAction' utils/action-executor.ts && grep -q 'EXECUTE_ACTION' entrypoints/content.ts && grep -q 'EXECUTE_ACTION' entrypoints/background.ts && grep -q 'click\\|type\\|scroll' utils/action-executor.ts && npm run build 2>&1 | grep -i 'error' | wc -l | xargs -I{} test {} -eq 0 && echo PASS || echo 'FAIL: Chrome 浏览器操作执行引擎缺失或构建失败'",
      "evolution_goal": "不考虑时间，使用最优解，让 agent 能使用工具替用户在浏览器上操作"
    },
    {
      "id": "evo_v6_002",
      "type": "coding",
      "title": "WebSocket 双向工具调用协议：新增 tool_execute/tool_result 消息类型，实现请求-响应匹配（requestId + Promise + 超时）",
      "status": "done",
      "depends_on": [
        "evo_v6_001"
      ],
      "acceptance": "Chrome 侧 ws-client.ts 或新建 utils/tool-bridge.ts：监听 WebSocket 收到的 tool_execute 消息（含 requestId/toolName/toolArgs），调用 background EXECUTE_ACTION 执行操作，将结果封装为 tool_result 消息（含 requestId/success/data/error）发回 VSCode；useWebSocket.ts 或 App.tsx 集成 tool_execute 消息处理（不阻塞聊天 UI）；VSCode 侧 ws-server.ts 新增 sendAndWait(ws, msg, timeoutMs) 方法：发送消息并创建 Promise，通过 requestId 匹配响应的 tool_result 消息，超时自动 reject（默认 30s）；ws-server.ts handleMessage 新增 tool_result 类型路由到 pending Promise resolver；消息协议文档化在代码注释中；双端构建通过",
      "acceptance_cmd": "cd packages/chrome-ext && grep -q 'tool_execute' src/ws-client.ts entrypoints/sidepanel/App.tsx hooks/useWebSocket.ts utils/tool-bridge.ts 2>/dev/null && grep -q 'tool_result' src/ws-client.ts entrypoints/sidepanel/App.tsx hooks/useWebSocket.ts utils/tool-bridge.ts 2>/dev/null && npm run build 2>&1 | grep -i 'error' | wc -l | xargs -I{} test {} -eq 0 && cd ../vscode-ext && grep -q 'tool_result' src/ws-server.ts && grep -q 'sendAndWait\\|requestId\\|pendingRequests' src/ws-server.ts && npm run compile 2>&1 | grep -c 'error' | xargs -I{} test {} -eq 0 && echo PASS || echo 'FAIL: WebSocket 工具调用协议缺失或构建失败'",
      "evolution_goal": "不考虑时间，使用最优解，让 agent 能使用工具替用户在浏览器上操作"
    },
    {
      "id": "evo_v6_003",
      "type": "coding",
      "title": "VSCode 侧浏览器工具提供者 BrowserToolProvider：定义原生浏览器工具注册表 + 通过 WebSocket 执行工具调用",
      "status": "done",
      "depends_on": [
        "evo_v6_002"
      ],
      "acceptance": "新建 packages/vscode-ext/src/browser-tools.ts，实现 BrowserToolProvider 类；定义 BrowserToolDef 接口（name/description/inputSchema）和完整工具列表：browser_click（点击元素）、browser_type（输入文本）、browser_navigate（导航到 URL）、browser_scroll（滚动页面）、browser_screenshot（截取可见区域截图）、browser_query_selector（查询 DOM 元素信息）、browser_get_text（获取元素文本内容）、browser_get_attribute（获取元素属性）、browser_wait（等待元素出现）、browser_highlight（高亮标记元素）；暴露 listTools() 返回工具定义列表、callTool(name, args) 通过 WsServer.sendAndWait 发送 tool_execute 并等待 tool_result；构造函数接收 WsServer 实例；与 McpClient 接口对齐（可互换使用）；在 extension.ts 中创建并注入；编译通过",
      "acceptance_cmd": "cd packages/vscode-ext && test -f src/browser-tools.ts && grep -q 'class BrowserToolProvider' src/browser-tools.ts && grep -q 'browser_click' src/browser-tools.ts && grep -q 'browser_type' src/browser-tools.ts && grep -q 'browser_navigate' src/browser-tools.ts && grep -q 'browser_screenshot' src/browser-tools.ts && grep -q 'listTools' src/browser-tools.ts && grep -q 'callTool' src/browser-tools.ts && grep -q 'BrowserToolProvider' src/extension.ts && npm run compile 2>&1 | grep -c 'error' | xargs -I{} test {} -eq 0 && echo PASS || echo 'FAIL: BrowserToolProvider 缺失或编译失败'",
      "evolution_goal": "不考虑时间，使用最优解，让 agent 能使用工具替用户在浏览器上操作"
    },
    {
      "id": "evo_v6_004",
      "type": "coding",
      "title": "AgentLoop 集成原生浏览器工具：多工具源支持（MCP + BrowserTools），无 MCP 也能进入 Agent 模式",
      "status": "done",
      "depends_on": [
        "evo_v6_003"
      ],
      "acceptance": "agent-loop.ts 修改：构造函数新增可选 browserToolProvider 参数；getToolDescriptions 合并 MCP 工具和 BrowserToolProvider 工具（browser_* 前缀工具优先用原生通道）；executeTool 根据工具名前缀路由：browser_* 开头的调用 BrowserToolProvider.callTool，其他调用 McpClient.callTool；message-handler.ts 修改：handleChat 判断条件从 mcpClient.connected 变为 mcpClient.connected || browserToolProvider.hasConnectedClient（只要 Chrome 有 WebSocket 连接就有原生浏览器工具可用）；AgentLoop 构造时同时传入 mcpClient 和 browserToolProvider；connection-tree.ts 新增原生浏览器工具状态节点（显示可用工具数和连接状态）；双端编译通过",
      "acceptance_cmd": "cd packages/vscode-ext && grep -q 'browserToolProvider\\|BrowserToolProvider' src/agent-loop.ts && grep -q 'browserToolProvider\\|BrowserToolProvider' src/message-handler.ts && grep -q 'browser_' src/agent-loop.ts && npm run compile 2>&1 | grep -c 'error' | xargs -I{} test {} -eq 0 && echo PASS || echo 'FAIL: AgentLoop 浏览器工具集成缺失或编译失败'",
      "evolution_goal": "不考虑时间，使用最优解，让 agent 能使用工具替用户在浏览器上操作"
    },
    {
      "id": "evo_v6_005",
      "type": "validate",
      "title": "浏览器操作工具全量验收：Chrome 操作执行器 + WebSocket 工具协议 + BrowserToolProvider + AgentLoop 集成，双端构建通过",
      "status": "done",
      "depends_on": [
        "evo_v6_004"
      ],
      "acceptance": "完整工具链验证：Chrome 侧 action-executor.ts 定义所有浏览器操作类型且 content.ts/background.ts 集成 EXECUTE_ACTION 路由；WebSocket 协议 tool_execute/tool_result 双向通信在 ws-client.ts 和 ws-server.ts 中实现（含 requestId 匹配和超时）；VSCode 侧 browser-tools.ts 注册 10 个原生浏览器工具并通过 WebSocket 调用；agent-loop.ts 支持多工具源路由（browser_* → 原生，其他 → MCP）；message-handler.ts 在 Chrome 连接时自动启用 Agent 模式（不再依赖 MCP）；connection-tree.ts 展示原生工具状态；双端构建均通过，无回归",
      "acceptance_cmd": "cd packages/chrome-ext && test -f utils/action-executor.ts && grep -q 'EXECUTE_ACTION' entrypoints/content.ts && grep -q 'EXECUTE_ACTION' entrypoints/background.ts && grep -q 'tool_execute' src/ws-client.ts hooks/useWebSocket.ts utils/tool-bridge.ts 2>/dev/null && npm run build 2>&1 | grep -i 'error' | wc -l | xargs -I{} test {} -eq 0 && cd ../vscode-ext && test -f src/browser-tools.ts && grep -q 'class BrowserToolProvider' src/browser-tools.ts && grep -q 'browserToolProvider\\|BrowserToolProvider' src/agent-loop.ts && grep -q 'browserToolProvider\\|BrowserToolProvider' src/message-handler.ts && grep -q 'sendAndWait\\|requestId' src/ws-server.ts && npm run compile 2>&1 | grep -c 'error' | xargs -I{} test {} -eq 0 && echo PASS || echo 'FAIL: 浏览器操作工具全量验收失败'",
      "evolution_goal": "不考虑时间，使用最优解，让 agent 能使用工具替用户在浏览器上操作"
    },
    {
      "id": "evo_v7_001",
      "type": "coding",
      "title": "项目 README.md：项目简介、架构概览、快速开始（安装+启动）、目录结构说明",
      "status": "done",
      "depends_on": [
        "evo_v6_005"
      ],
      "acceptance": "根目录创建 docs/README.md（或更新已有 README.md）；包含：项目简介（一句话描述 Chrome+VSCode Agent 系统）、系统架构图（文字版，Chrome Side Panel ↔ WebSocket ↔ VSCode LM）、快速开始（前置条件 Node18+/pnpm/VSCode+Copilot Chat、安装步骤 pnpm install、分别启动两个插件、Chrome 加载未打包扩展路径）、目录结构树形说明（packages/chrome-ext 和 packages/vscode-ext 的核心文件简述）；文档语言中英文皆可，使用 Markdown 格式",
      "acceptance_cmd": "test -f docs/README.md && grep -q 'Chrome' docs/README.md && grep -q 'VSCode\\|vscode' docs/README.md && grep -q 'WebSocket\\|websocket\\|7777' docs/README.md && grep -q 'pnpm\\|npm' docs/README.md && grep -q 'install\\|安装\\|Quick Start\\|快速开始' docs/README.md && echo PASS || echo 'FAIL: README.md 缺失或内容不完整'",
      "evolution_goal": "希望有简要的功能使用文档及使用案例，指导用户使用"
    },
    {
      "id": "evo_v7_002",
      "type": "coding",
      "title": "Chrome 插件功能使用文档：Side Panel 对话、模型选择、多会话管理、斜杠命令、快捷键",
      "status": "done",
      "depends_on": [
        "evo_v7_001"
      ],
      "acceptance": "创建 docs/chrome-extension-guide.md；涵盖：Side Panel 打开方式（Chrome 工具栏图标或右键菜单）；对话流程（输入消息 → 选择模型 → 发送 → 查看流式回复）；模型选择器使用说明（下拉选择可用 LLM）；多会话管理（新建会话、切换会话、删除会话、会话列表侧栏）；斜杠命令列表（/new /clear /models /help 的用途说明）；键盘快捷键列表（Cmd+L 清空、ArrowUp 历史、Cmd+Enter 发送、Cmd+Shift+O 新建会话）；页面上下文感知说明（自动附带当前 URL/标题/选中文本）；配有简要操作步骤编号",
      "acceptance_cmd": "test -f docs/chrome-extension-guide.md && grep -q 'Side Panel\\|side panel\\|侧边栏' docs/chrome-extension-guide.md && grep -q '/new\\|/clear' docs/chrome-extension-guide.md && grep -q '会话\\|session\\|conversation' docs/chrome-extension-guide.md && grep -q '快捷键\\|shortcut\\|Cmd\\|Ctrl' docs/chrome-extension-guide.md && grep -q '模型\\|model' docs/chrome-extension-guide.md && echo PASS || echo 'FAIL: Chrome 插件使用文档缺失或内容不完整'",
      "evolution_goal": "希望有简要的功能使用文档及使用案例，指导用户使用"
    },
    {
      "id": "evo_v7_003",
      "type": "coding",
      "title": "VSCode 插件功能使用文档：调试视图、连接 MCP、Agent 模式、浏览器工具说明",
      "status": "done",
      "depends_on": [
        "evo_v7_001"
      ],
      "acceptance": "创建 docs/vscode-extension-guide.md；涵盖：插件安装与激活方式（从 packages/vscode-ext 加载开发版或 F5 调试启动）；WebSocket 服务器说明（默认端口 7777、browserAgent.port 配置项）；Activity Bar 调试视图使用：连接状态 TreeView（WebSocket/MCP/模型信息）、消息检查器（查看 WebSocket 消息流、点击查看完整 JSON）、Agent 循环可视化（查看 ReAct 步骤历史）；连接 DevTools MCP 说明（命令 Browser Agent: 连接 DevTools MCP）；Agent 模式触发条件（连接 MCP 或 Chrome 浏览器已连接即自动启用）；原生浏览器工具列表（browser_click/type/navigate/scroll/screenshot 等 10+ 工具的简要说明）；常用命令列表（提问/连接 DevTools/生成报告/清空日志）",
      "acceptance_cmd": "test -f docs/vscode-extension-guide.md && grep -q 'Activity Bar\\|TreeView\\|调试视图\\|Debug' docs/vscode-extension-guide.md && grep -q 'MCP\\|DevTools' docs/vscode-extension-guide.md && grep -q 'browser_click\\|browser_navigate\\|浏览器工具\\|Browser Tool' docs/vscode-extension-guide.md && grep -q 'Agent\\|agent' docs/vscode-extension-guide.md && grep -q '7777\\|port\\|端口' docs/vscode-extension-guide.md && echo PASS || echo 'FAIL: VSCode 插件使用文档缺失或内容不完整'",
      "evolution_goal": "希望有简要的功能使用文档及使用案例，指导用户使用"
    },
    {
      "id": "evo_v7_004",
      "type": "coding",
      "title": "使用案例文档：5 个典型场景（基础对话、浏览器自动操作、深度报告、调试排查、多会话工作流）",
      "status": "done",
      "depends_on": [
        "evo_v7_002",
        "evo_v7_003"
      ],
      "acceptance": "创建 docs/use-cases.md；包含 5 个完整使用案例，每个案例包含：场景标题、适用场景描述、操作步骤（编号列表）、预期结果。案例 1：基础对话问答（打开 Side Panel → 选模型 → 发送问题 → 获得回答）；案例 2：让 Agent 自动操作浏览器（打开目标页面 → 描述操作意图如'帮我填写这个表单' → Agent 自动调用 browser_click/type 等工具 → 观察 Agent Step 展示）；案例 3：生成深度报告（打开多个相关页面 → 执行'生成深度报告'命令 → Agent 自动探索页面并生成 Markdown 报告）；案例 4：调试 WebSocket 通信问题（打开 VSCode Activity Bar → 查看连接状态 → 检查消息日志 → 定位问题）；案例 5：多会话工作流（使用 /new 创建新会话 → 不同会话处理不同任务 → 通过侧栏切换会话 → 历史记录持久化）",
      "acceptance_cmd": "test -f docs/use-cases.md && grep -c '案例\\|Case\\|场景\\|Scenario\\|##' docs/use-cases.md | xargs -I{} test {} -ge 5 && grep -q 'browser_click\\|browser_type\\|自动操作\\|automat' docs/use-cases.md && grep -q '报告\\|report\\|Report' docs/use-cases.md && grep -q '调试\\|debug\\|Debug' docs/use-cases.md && grep -q '会话\\|session\\|conversation' docs/use-cases.md && echo PASS || echo 'FAIL: 使用案例文档缺失或案例数量不足'",
      "evolution_goal": "希望有简要的功能使用文档及使用案例，指导用户使用"
    },
    {
      "id": "evo_v7_005",
      "type": "validate",
      "title": "文档全量验收：README + Chrome 指南 + VSCode 指南 + 使用案例，文档齐全且内容覆盖所有核心功能",
      "status": "done",
      "depends_on": [
        "evo_v7_004"
      ],
      "acceptance": "docs/ 目录下有 4 个文档文件（README.md、chrome-extension-guide.md、vscode-extension-guide.md、use-cases.md）；README 包含架构和快速开始；Chrome 指南覆盖对话/模型/会话/命令/快捷键；VSCode 指南覆盖调试视图/MCP/Agent/工具；使用案例不少于 5 个且每个含操作步骤；双端构建通过无回归",
      "acceptance_cmd": "test -d docs && test -f docs/README.md && test -f docs/chrome-extension-guide.md && test -f docs/vscode-extension-guide.md && test -f docs/use-cases.md && grep -q 'WebSocket\\|websocket' docs/README.md && grep -q '/new\\|/clear' docs/chrome-extension-guide.md && grep -q 'TreeView\\|Activity Bar\\|调试' docs/vscode-extension-guide.md && grep -c '##' docs/use-cases.md | xargs -I{} test {} -ge 5 && cd packages/chrome-ext && npm run build 2>&1 | grep -i 'error' | wc -l | xargs -I{} test {} -eq 0 && cd ../vscode-ext && npm run compile 2>&1 | grep -c 'error' | xargs -I{} test {} -eq 0 && echo PASS || echo 'FAIL: 文档全量验收失败'",
      "evolution_goal": "希望有简要的功能使用文档及使用案例，指导用户使用"
    },
    {
      "id": "evo_v8_001",
      "type": "coding",
      "title": "Skill 数据模型 + SkillRegistry + 5 个内置预设 Skill 定义",
      "status": "done",
      "depends_on": [
        "evo_v7_005"
      ],
      "acceptance": "新建 packages/vscode-ext/src/skill-registry.ts；定义 Skill 接口（遵循 MCP Tool Schema 风格）：name（唯一标识）、displayName（展示名称）、description（描述）、category（分类：preset/custom）、enabled（启用状态）、parameters（输入参数 JSON Schema，如 url/selector 等变量）、steps（SkillStep[] 有序步骤列表，每步包含 toolName + argsTemplate 模板）；定义 SkillStep 接口：toolName（browser_* 工具名）、argsTemplate（参数模板，支持 {{param}} 变量插值）、description（步骤描述）、optional（是否可跳过）；实现 SkillRegistry 类：loadSkills()（从 workspace configuration browserAgent.skills 加载自定义 skill + 合并内置 preset）、saveSkills()（持久化自定义 skill 到 workspace config）、getAll()、getByName()、addSkill()、removeSkill()、toggleEnabled()；内置 5 个预设 Skill：1) navigate_to_url（导航到指定 URL，参数 url）、2) organize_tabs（整理当前标签页：获取所有 tab 信息 + 按域名分组）、3) translate_page（翻译当前页面主要内容：提取文本 + LLM 翻译 + 高亮展示）、4) extract_page_data（提取页面结构化数据：标题/链接/图片/表格）、5) smart_form_fill（智能表单填充：识别表单字段 + 根据用户描述自动填写）；在 extension.ts 中创建 SkillRegistry 实例并注入相关服务；编译通过",
      "acceptance_cmd": "cd packages/vscode-ext && test -f src/skill-registry.ts && grep -q 'interface Skill' src/skill-registry.ts && grep -q 'interface SkillStep' src/skill-registry.ts && grep -q 'class SkillRegistry' src/skill-registry.ts && grep -q 'navigate_to_url' src/skill-registry.ts && grep -q 'organize_tabs' src/skill-registry.ts && grep -q 'translate_page' src/skill-registry.ts && grep -q 'extract_page_data' src/skill-registry.ts && grep -q 'smart_form_fill' src/skill-registry.ts && grep -q 'SkillRegistry' src/extension.ts && npm run compile 2>&1 | grep -c 'error' | xargs -I{} test {} -eq 0 && echo PASS || echo 'FAIL: SkillRegistry 缺失或编译失败'",
      "evolution_goal": "不考虑时间，使用最优解，希望能遵循 skill， mcp 规范，有个 vsocde 视图配置配置，可以预设一批操作浏览器的 skill，进入指定页面，整理 tab，友好型将页面翻译，等"
    },
    {
      "id": "evo_v8_002",
      "type": "coding",
      "title": "VSCode Skill 管理 TreeView：Activity Bar 新增 Skill 视图，展示/运行/管理所有 Skill",
      "status": "done",
      "depends_on": [
        "evo_v8_001"
      ],
      "acceptance": "新建 packages/vscode-ext/src/skill-tree.ts；实现 SkillTreeDataProvider（extends TreeDataProvider）：按 category 分组展示 Skill（preset 预设 / custom 自定义两组）；每个 Skill 节点显示：图标（▶ 可用 / ⏸ 禁用）、displayName、description（tooltip）；TreeItem 支持 inline 按钮：运行（播放图标）、启用/禁用切换；注册 3 个命令：browser-agent.runSkill（运行指定 Skill，弹出参数输入框 QuickPick/InputBox）、browser-agent.toggleSkill（切换 enabled 状态）、browser-agent.addCustomSkill（打开 JSON 编辑器创建自定义 Skill）；package.json contributes 新增：views 中添加 browser-agent-skills 视图、commands 中添加 3 个命令、menus 中配置 view/item/context 和 inline 按钮；extension.ts 中创建 TreeView 并绑定 SkillRegistry；onDidChangeTreeData 在 SkillRegistry 变更时刷新；编译通过",
      "acceptance_cmd": "cd packages/vscode-ext && test -f src/skill-tree.ts && grep -q 'class SkillTreeDataProvider' src/skill-tree.ts && grep -q 'browser-agent-skills' package.json && grep -q 'browser-agent.runSkill' package.json && grep -q 'browser-agent.toggleSkill' package.json && grep -q 'browser-agent.addCustomSkill' package.json && grep -q 'SkillTreeDataProvider' src/extension.ts && npm run compile 2>&1 | grep -c 'error' | xargs -I{} test {} -eq 0 && echo PASS || echo 'FAIL: Skill TreeView 缺失或编译失败'",
      "evolution_goal": "不考虑时间，使用最优解，希望能遵循 skill， mcp 规范，有个 vsocde 视图配置配置，可以预设一批操作浏览器的 skill，进入指定页面，整理 tab，友好型将页面翻译，等"
    },
    {
      "id": "evo_v8_003",
      "type": "coding",
      "title": "Skill 执行引擎 SkillRunner + AgentLoop 集成 run_skill 工具",
      "status": "done",
      "depends_on": [
        "evo_v8_002"
      ],
      "acceptance": "新建 packages/vscode-ext/src/skill-runner.ts；实现 SkillRunner 类：构造函数接收 BrowserToolProvider + McpClient + LmService + OutputChannel；execute(skill, params, onProgress, token) 方法：1) 校验 skill.enabled 和参数完整性，2) 按 steps 顺序执行每一步，3) 每步将 argsTemplate 中的 {{param}} 替换为实际参数值，4) 通过 BrowserToolProvider.callTool 或 McpClient.callTool 执行工具调用，5) onProgress 回调报告每步进度（stepIndex/totalSteps/status/result），6) 支持 CancellationToken 中断，7) 步骤失败时根据 optional 标记决定跳过或终止，8) 返回 SkillRunResult（success/stepResults/summary）；agent-loop.ts 集成：在 getToolDescriptions 中注册 run_skill 工具（参数为 skill_name + params JSON），executeTool 中路由 run_skill 到 SkillRunner.execute；skill-tree.ts 中 runSkill 命令调用 SkillRunner；编译通过",
      "acceptance_cmd": "cd packages/vscode-ext && test -f src/skill-runner.ts && grep -q 'class SkillRunner' src/skill-runner.ts && grep -q 'execute' src/skill-runner.ts && grep -q 'onProgress\\|SkillProgress' src/skill-runner.ts && grep -q 'run_skill' src/agent-loop.ts && npm run compile 2>&1 | grep -c 'error' | xargs -I{} test {} -eq 0 && echo PASS || echo 'FAIL: SkillRunner 缺失或 AgentLoop 集成失败'",
      "evolution_goal": "不考虑时间，使用最优解，希望能遵循 skill， mcp 规范，有个 vsocde 视图配置配置，可以预设一批操作浏览器的 skill，进入指定页面，整理 tab，友好型将页面翻译，等"
    },
    {
      "id": "evo_v8_004",
      "type": "coding",
      "title": "Chrome Side Panel Skill 面板 + WebSocket 协议扩展：展示可用 Skill、一键触发、执行进度",
      "status": "done",
      "depends_on": [
        "evo_v8_003"
      ],
      "acceptance": "Chrome 侧新增 Skill 面板组件和 WebSocket 协议扩展；新建 packages/chrome-ext/components/SkillPanel.tsx：顶部 Tab 切换（Chat / Skills 两个视图）；Skill 卡片列表：每个 Skill 显示 displayName + description + category 标签 + 运行按钮；点击运行时弹出参数输入弹窗（根据 skill.parameters 动态生成输入字段）；执行中显示进度条/步骤列表（当前步骤高亮、已完成步骤打勾、失败步骤标红）；WebSocket 协议新增 4 个消息类型：skill_list（请求可用 Skill 列表）→ skill_list_result（返回 Skill 数组）；skill_execute（触发执行，含 skillName + params）→ skill_progress（每步进度推送）→ skill_complete（执行完成/失败）；VSCode 侧 message-handler.ts 新增 skill_list / skill_execute 消息处理：skill_list 从 SkillRegistry.getAll() 返回；skill_execute 调用 SkillRunner.execute 并通过 skill_progress 实时推送进度；App.tsx 集成 Tab 切换和 SkillPanel 组件；hooks/useWebSocket.ts 处理 skill 相关消息；双端构建通过",
      "acceptance_cmd": "cd packages/chrome-ext && test -f components/SkillPanel.tsx && grep -q 'SkillPanel\\|skill' components/SkillPanel.tsx && grep -q 'skill_list\\|skill_execute' hooks/useWebSocket.ts src/ws-client.ts components/SkillPanel.tsx 2>/dev/null && npm run build 2>&1 | grep -i 'error' | wc -l | xargs -I{} test {} -eq 0 && cd ../vscode-ext && grep -q 'skill_list\\|skill_execute' src/message-handler.ts && grep -q 'skill_progress\\|skill_complete' src/message-handler.ts && npm run compile 2>&1 | grep -c 'error' | xargs -I{} test {} -eq 0 && echo PASS || echo 'FAIL: Chrome Skill 面板或 WebSocket 协议扩展缺失'",
      "evolution_goal": "不考虑时间，使用最优解，希望能遵循 skill， mcp 规范，有个 vsocde 视图配置配置，可以预设一批操作浏览器的 skill，进入指定页面，整理 tab，友好型将页面翻译，等"
    },
    {
      "id": "evo_v8_005",
      "type": "validate",
      "title": "Skill 系统全量验收：数据模型 + TreeView + 执行引擎 + Chrome 面板 + 5 个预设 Skill，双端构建通过",
      "status": "done",
      "depends_on": [
        "evo_v8_004"
      ],
      "acceptance": "完整 Skill 系统验证：skill-registry.ts 定义 Skill/SkillStep 接口并内置 5 个预设 Skill（navigate_to_url/organize_tabs/translate_page/extract_page_data/smart_form_fill）；skill-tree.ts 实现 SkillTreeDataProvider 按分类展示 Skill 列表（preset/custom 分组）；package.json 注册 browser-agent-skills TreeView + runSkill/toggleSkill/addCustomSkill 命令；skill-runner.ts 实现 SkillRunner 按步骤执行 Skill（变量插值 + 进度回调 + 取消支持 + 可选步骤跳过）；agent-loop.ts 注册 run_skill 工具供 LLM 自主调用 Skill；Chrome 侧 SkillPanel.tsx 实现 Skill 卡片列表 + 参数输入 + 执行进度展示；WebSocket 协议支持 skill_list/skill_execute/skill_progress/skill_complete 消息；message-handler.ts 处理 Skill 相关消息路由；双端构建均通过无回归",
      "acceptance_cmd": "cd packages/vscode-ext && test -f src/skill-registry.ts && test -f src/skill-tree.ts && test -f src/skill-runner.ts && grep -q 'class SkillRegistry' src/skill-registry.ts && grep -q 'navigate_to_url' src/skill-registry.ts && grep -q 'organize_tabs' src/skill-registry.ts && grep -q 'translate_page' src/skill-registry.ts && grep -q 'class SkillTreeDataProvider' src/skill-tree.ts && grep -q 'class SkillRunner' src/skill-runner.ts && grep -q 'run_skill' src/agent-loop.ts && grep -q 'browser-agent-skills' package.json && grep -q 'skill_list\\|skill_execute' src/message-handler.ts && npm run compile 2>&1 | grep -c 'error' | xargs -I{} test {} -eq 0 && cd ../chrome-ext && test -f components/SkillPanel.tsx && grep -q 'skill' components/SkillPanel.tsx && npm run build 2>&1 | grep -i 'error' | wc -l | xargs -I{} test {} -eq 0 && echo PASS || echo 'FAIL: Skill 系统全量验收失败'",
      "evolution_goal": "不考虑时间，使用最优解，希望能遵循 skill， mcp 规范，有个 vsocde 视图配置配置，可以预设一批操作浏览器的 skill，进入指定页面，整理 tab，友好型将页面翻译，等"
    },
    {
      "id": "evo_v9_001",
      "type": "coding",
      "title": "React Error Boundary + 全局错误拦截层：防止组件崩溃白屏，自动恢复不丢会话",
      "status": "done",
      "depends_on": [
        "evo_v8_005"
      ],
      "acceptance": "Chrome 侧新增 ErrorBoundary 组件包裹 App 根节点；崩溃时显示友好 fallback UI（含错误摘要 + '重新加载' 按钮）；添加 window.onerror 和 unhandledrejection 全局监听器捕获异步错误并记录到 errorLog 状态；错误恢复后保留当前会话 ID 和已持久化的消息；所有 hooks（useChat/useWebSocket/useChatStorage）的关键操作均有 try-catch 防护",
      "acceptance_cmd": "cd packages/chrome-ext && test -f components/ErrorBoundary.tsx && grep -q 'componentDidCatch\\|ErrorBoundary' components/ErrorBoundary.tsx && grep -q 'ErrorBoundary' entrypoints/sidepanel/App.tsx && grep -q 'onerror\\|unhandledrejection' entrypoints/sidepanel/App.tsx && npm run build 2>&1 | grep -i 'error TS' | wc -l | xargs -I{} test {} -eq 0 && echo PASS || echo 'FAIL: Error Boundary 未正确实现'",
      "evolution_goal": "不考虑时间，使用最优解，希望 chrome 对话体验要稳定，尽可能避免出错。可以考虑补充相关开关或者 tab 页面等，背后执行逻辑方便用户查看，或者说方便 debug"
    },
    {
      "id": "evo_v9_002",
      "type": "coding",
      "title": "Toast 通知系统 + 消息发送重试机制：非阻塞错误提示，失败消息可一键重试",
      "status": "done",
      "depends_on": [
        "evo_v9_001"
      ],
      "acceptance": "新增 Toast 组件（支持 success/warning/error/info 四种类型，3 秒自动消失，可手动关闭）；新增 useToast hook 提供 showToast 方法供全局调用；WebSocket 断连、消息发送失败、工具执行异常、存储错误等场景均触发对应 toast 通知；发送失败的消息显示红色边框 + '重试' 按钮；点击重试按钮重新发送原始消息；Message 类型新增 status 字段（sending/sent/failed）；ChatInput 发送时消息先标记 sending，收到响应后标记 sent，超时或错误标记 failed",
      "acceptance_cmd": "cd packages/chrome-ext && test -f components/Toast.tsx && test -f hooks/useToast.ts && grep -q 'Toast\\|toast' components/Toast.tsx && grep -q 'showToast' hooks/useToast.ts && grep -q 'retry\\|重试\\|resend' hooks/useChat.ts && grep -q 'sending\\|sent\\|failed' utils/message-factory.ts && npm run build 2>&1 | grep -i 'error TS' | wc -l | xargs -I{} test {} -eq 0 && echo PASS || echo 'FAIL: Toast 通知系统或重试机制未正确实现'",
      "evolution_goal": "不考虑时间，使用最优解，希望 chrome 对话体验要稳定，尽可能避免出错。可以考虑补充相关开关或者 tab 页面等，背后执行逻辑方便用户查看，或者说方便 debug"
    },
    {
      "id": "evo_v9_003",
      "type": "coding",
      "title": "WebSocket 连接健壮性增强：心跳检测 + 连接状态机 + 流式中断恢复 + 手动重连按钮",
      "status": "done",
      "depends_on": [
        "evo_v9_002"
      ],
      "acceptance": "ws-client.ts 新增心跳机制（每 15 秒发送 ping，10 秒内未收到 pong 则判定连接失效并触发重连）；连接状态从简单的 connected/disconnected 扩展为完整状态机（disconnected/connecting/connected/reconnecting/failed）；流式传输中断时自动保留已接收的部分内容并显示 '⚠️ 连接中断，回复不完整' 提示；连接指示器升级为可点击组件，显示详细状态弹窗（重连次数、最后活跃时间、延迟）；弹窗包含手动 '重新连接' 按钮；所有入站消息增加基础类型校验（必须包含 type 字段）",
      "acceptance_cmd": "cd packages/chrome-ext && grep -q 'ping\\|heartbeat' src/ws-client.ts && grep -q 'reconnecting\\|failed' src/ws-client.ts && grep -q '连接中断\\|不完整' hooks/useChat.ts && grep -q '重新连接\\|reconnect' entrypoints/sidepanel/App.tsx && npm run build 2>&1 | grep -i 'error TS' | wc -l | xargs -I{} test {} -eq 0 && echo PASS || echo 'FAIL: WebSocket 健壮性增强未正确实现'",
      "evolution_goal": "不考虑时间，使用最优解，希望 chrome 对话体验要稳定，尽可能避免出错。可以考虑补充相关开关或者 tab 页面等，背后执行逻辑方便用户查看，或者说方便 debug"
    },
    {
      "id": "evo_v9_004",
      "type": "coding",
      "title": "Chrome Side Panel 新增 Debug 调试面板：实时消息日志 + 连接仪表盘 + 执行时间线 + 开关控制",
      "status": "done",
      "depends_on": [
        "evo_v9_003"
      ],
      "acceptance": "App.tsx 顶部 Tab 栏新增第三个 '调试' Tab（Chat | Skills | Debug）；Debug 面板包含三个折叠区域：(1) 连接仪表盘 — 显示 WebSocket 状态、重连次数、心跳延迟、会话 ID、已选模型；(2) 消息日志 — 环形缓冲区保留最近 50 条 WebSocket 消息，每条显示方向箭头（↑发送/↓接收）、类型标签、时间戳，点击可展开完整 payload JSON；(3) 执行时间线 — 展示当前/最近一次 Agent 执行的 think/act/observe 步骤链，每步标记耗时；面板顶部提供开关：'详细日志'（控制是否记录 payload）、'自动滚动'；新增 useDebugLog hook 管理日志状态；构建通过无回归",
      "acceptance_cmd": "cd packages/chrome-ext && test -f components/DebugPanel.tsx && test -f hooks/useDebugLog.ts && grep -q 'DebugPanel\\|debug' components/DebugPanel.tsx && grep -q '调试\\|Debug' entrypoints/sidepanel/App.tsx && grep -q 'useDebugLog' hooks/useDebugLog.ts && grep -q '消息日志\\|messageLog\\|MessageLog' components/DebugPanel.tsx && npm run build 2>&1 | grep -i 'error TS' | wc -l | xargs -I{} test {} -eq 0 && echo PASS || echo 'FAIL: Debug 调试面板未正确实现'",
      "evolution_goal": "不考虑时间，使用最优解，希望 chrome 对话体验要稳定，尽可能避免出错。可以考虑补充相关开关或者 tab 页面等，背后执行逻辑方便用户查看，或者说方便 debug"
    },
    {
      "id": "evo_v9_005",
      "type": "validate",
      "title": "稳定性体系全量验收：Error Boundary + Toast + WebSocket 健壮性 + Debug 面板，双端构建通过",
      "status": "done",
      "depends_on": [
        "evo_v9_004"
      ],
      "acceptance": "全链路验证：ErrorBoundary 包裹 App 根节点且含 fallback UI；Toast 组件支持四种类型且 useToast 提供全局 showToast；消息发送失败可重试（Message.status 字段 + 重试按钮）；ws-client 心跳机制正常（ping/pong 间隔）；连接状态包含 reconnecting/failed 等完整状态；流式中断有保留和提示；连接指示器可点击显示详情和手动重连；Debug Tab 存在且含连接仪表盘 + 消息日志 + 执行时间线三个区域；useDebugLog hook 正常工作；双端构建均通过无 TS 错误",
      "acceptance_cmd": "cd packages/chrome-ext && test -f components/ErrorBoundary.tsx && test -f components/Toast.tsx && test -f hooks/useToast.ts && test -f components/DebugPanel.tsx && test -f hooks/useDebugLog.ts && grep -q 'ErrorBoundary' entrypoints/sidepanel/App.tsx && grep -q 'Toast' components/Toast.tsx && grep -q 'heartbeat\\|ping' src/ws-client.ts && grep -q '调试\\|Debug' entrypoints/sidepanel/App.tsx && npm run build 2>&1 | grep -i 'error TS' | wc -l | xargs -I{} test {} -eq 0 && cd ../vscode-ext && npm run compile 2>&1 | grep -i 'error' | wc -l | xargs -I{} test {} -eq 0 && echo PASS || echo 'FAIL: 稳定性体系全量验收失败'",
      "evolution_goal": "不考虑时间，使用最优解，希望 chrome 对话体验要稳定，尽可能避免出错。可以考虑补充相关开关或者 tab 页面等，背后执行逻辑方便用户查看，或者说方便 debug"
    },
    {
      "id": "evo_v10_001",
      "type": "coding",
      "title": "VSCode 侧 LLM 请求细节采集模块：创建 LlmRequestCollector 类，在 lm-service / agent-loop / message-handler 中采集 model/systemPrompt/messages/timing/response 完整链路数据",
      "status": "done",
      "depends_on": [
        "evo_v9_002"
      ],
      "acceptance": "packages/vscode-ext/src/ 下新增 llm-request-collector.ts，导出 LlmRequestCollector 类和 LlmRequestDetail 接口；message-handler.ts 中集成采集器，每次 chat/agent 请求结束后能生成结构化的 LLM 请求细节对象；VSCode 插件编译通过",
      "acceptance_cmd": "cd packages/vscode-ext && test -f src/llm-request-collector.ts && grep -r 'LlmRequestCollector\\|LlmRequestDetail' src/llm-request-collector.ts src/message-handler.ts && npm run compile 2>&1 | tail -3 && echo PASS || echo FAIL",
      "evolution_goal": "在对话窗口提供按钮，可以下载完整的 llm 请求细节，方便 debugger 也方便分析优化"
    },
    {
      "id": "evo_v10_002",
      "type": "coding",
      "title": "WebSocket 协议扩展 + Chrome 侧数据接收：chat_response_end / agent_complete payload 附加 llmDetail 字段，Chrome useChat hook 接收并关联到对应 assistant 消息",
      "status": "done",
      "depends_on": [
        "evo_v10_001"
      ],
      "acceptance": "message-handler.ts 在 chat_response_end 和 agent_complete 消息的 payload 中包含 llmDetail 字段；message-factory.ts 的 Message 类型扩展可选 llmDetail 属性；useChat.ts 在 chat_response_end / agent_complete 处理中将 llmDetail 存入对应 assistant 消息",
      "acceptance_cmd": "cd packages/vscode-ext && grep -r 'llmDetail' src/message-handler.ts && cd ../../packages/chrome-ext && grep -r 'llmDetail' utils/message-factory.ts hooks/useChat.ts && echo PASS || echo FAIL",
      "evolution_goal": "在对话窗口提供按钮，可以下载完整的 llm 请求细节，方便 debugger 也方便分析优化"
    },
    {
      "id": "evo_v10_003",
      "type": "coding",
      "title": "Chrome 侧 JSON 下载工具函数：创建 download-llm-detail.ts，将 LlmDetail 数据格式化为可读 JSON 并触发浏览器文件下载（含 timestamp 文件名）",
      "status": "done",
      "depends_on": [
        "evo_v10_002"
      ],
      "acceptance": "packages/chrome-ext/utils/download-llm-detail.ts 存在，导出 downloadLlmDetail 函数，能将 LlmDetail 对象转为格式化 JSON 字符串并通过 Blob + URL.createObjectURL 触发浏览器下载",
      "acceptance_cmd": "cd packages/chrome-ext && test -f utils/download-llm-detail.ts && grep -r 'downloadLlmDetail\\|Blob\\|createObjectURL' utils/download-llm-detail.ts && echo PASS || echo FAIL",
      "evolution_goal": "在对话窗口提供按钮，可以下载完整的 llm 请求细节，方便 debugger 也方便分析优化"
    },
    {
      "id": "evo_v10_004",
      "type": "coding",
      "title": "Chrome UI 下载按钮：MessageBubble assistant 消息底部添加下载图标按钮，有 llmDetail 时激活，点击调用 downloadLlmDetail 触发 JSON 下载",
      "status": "done",
      "depends_on": [
        "evo_v10_003"
      ],
      "acceptance": "MessageBubble.tsx 接收 llmDetail prop，assistant 消息工具栏显示下载按钮图标，点击触发 JSON 文件下载；无 llmDetail 时按钮不显示或禁用；App.tsx 传递 llmDetail 到 MessageBubble",
      "acceptance_cmd": "cd packages/chrome-ext && grep -r 'downloadLlmDetail\\|llmDetail' components/MessageBubble.tsx entrypoints/sidepanel/App.tsx && echo PASS || echo FAIL",
      "evolution_goal": "在对话窗口提供按钮，可以下载完整的 llm 请求细节，方便 debugger 也方便分析优化"
    },
    {
      "id": "evo_v10_005",
      "type": "validate",
      "title": "LLM 请求细节下载功能全量验收：双端构建通过，assistant 消息可下载包含 model/systemPrompt/messages/response/timing 的完整 JSON",
      "status": "done",
      "depends_on": [
        "evo_v10_004"
      ],
      "acceptance": "VSCode 插件 compile 通过，Chrome 插件 build 通过；LlmRequestCollector 采集完整数据；WebSocket 协议正确传输 llmDetail；MessageBubble 有下载按钮；下载 JSON 包含 model/systemPrompt/messages/response/timing/agentSteps 字段",
      "acceptance_cmd": "cd packages/vscode-ext && npm run compile 2>&1 | tail -3 && cd ../../packages/chrome-ext && npx wxt build 2>&1 | tail -5 && grep -r 'LlmRequestCollector' ../vscode-ext/src/llm-request-collector.ts && grep -r 'llmDetail' ../chrome-ext/utils/message-factory.ts ../chrome-ext/components/MessageBubble.tsx ../vscode-ext/src/message-handler.ts && echo PASS || echo FAIL",
      "evolution_goal": "在对话窗口提供按钮，可以下载完整的 llm 请求细节，方便 debugger 也方便分析优化"
    },
    {
      "id": "evo_v11_001",
      "type": "coding",
      "title": "创建 UserDataManager 服务类 + 添加 browserAgent.userDataDir 配置 schema",
      "status": "done",
      "depends_on": [
        "evo_v10_005"
      ],
      "acceptance": "新增 packages/vscode-ext/src/user-data-manager.ts；UserDataManager 支持 init()（创建目录结构）、getPath()、readJSON()、writeJSON() 方法；package.json contributes.configuration 中新增 browserAgent.userDataDir 配置项（type: string，default: ~/.browser-agent）；compile 通过",
      "acceptance_cmd": "cd packages/vscode-ext && npm run compile 2>&1 | tail -3 && test -f src/user-data-manager.ts && grep -q 'UserDataManager' src/user-data-manager.ts && grep -q 'userDataDir' package.json && echo PASS || echo FAIL",
      "evolution_goal": "vsocde 插件可以配置用户信息的目录，默认配置在 ～ 目录下；可以存放 skills 等等需要持久化的信息"
    },
    {
      "id": "evo_v11_002",
      "type": "coding",
      "title": "在 extension.ts 中集成 UserDataManager 生命周期：激活时初始化目录，监听配置变更",
      "status": "done",
      "depends_on": [
        "evo_v11_001"
      ],
      "acceptance": "extension.ts 在 activate() 中创建 UserDataManager 实例并调用 init()；监听 browserAgent.userDataDir 配置变更事件，变更时重新初始化；默认目录 ~/.browser-agent/ 下自动创建 skills/、config/、sessions/ 子目录；compile 通过",
      "acceptance_cmd": "cd packages/vscode-ext && npm run compile 2>&1 | tail -3 && grep -q 'UserDataManager' src/extension.ts && grep -q 'onDidChangeConfiguration' src/extension.ts && echo PASS || echo FAIL",
      "evolution_goal": "vsocde 插件可以配置用户信息的目录，默认配置在 ～ 目录下；可以存放 skills 等等需要持久化的信息"
    },
    {
      "id": "evo_v11_003",
      "type": "coding",
      "title": "重构 SkillRegistry 持久化：从 workspace config 迁移到 UserDataManager 文件存储",
      "status": "done",
      "depends_on": [
        "evo_v11_002"
      ],
      "acceptance": "SkillRegistry 构造函数接受 UserDataManager 参数；loadSkills() 从 ~/.browser-agent/skills/custom-skills.json 加载自定义 Skill；saveSkills() 写入同路径；预设开关存储到 ~/.browser-agent/skills/preset-overrides.json；首次加载时自动迁移旧 workspace config 数据；compile 通过",
      "acceptance_cmd": "cd packages/vscode-ext && npm run compile 2>&1 | tail -3 && grep -q 'UserDataManager' src/skill-registry.ts && grep -q 'custom-skills.json' src/skill-registry.ts && grep -q 'preset-overrides.json' src/skill-registry.ts && echo PASS || echo FAIL",
      "evolution_goal": "vsocde 插件可以配置用户信息的目录，默认配置在 ～ 目录下；可以存放 skills 等等需要持久化的信息"
    },
    {
      "id": "evo_v11_004",
      "type": "coding",
      "title": "添加用户数据目录管理命令 + ConnectionTree 状态展示",
      "status": "done",
      "depends_on": [
        "evo_v11_003"
      ],
      "acceptance": "注册 browserAgent.openUserDataDir 命令（在文件管理器中打开用户数据目录）；注册 browserAgent.revealUserDataDir 命令（在 VSCode 中打开目录）；ConnectionTreeDataProvider 显示当前 userDataDir 路径和磁盘占用；compile 通过",
      "acceptance_cmd": "cd packages/vscode-ext && npm run compile 2>&1 | tail -3 && grep -q 'openUserDataDir' src/command-registry.ts && grep -q 'userDataDir\\|UserData' src/connection-tree.ts && echo PASS || echo FAIL",
      "evolution_goal": "vsocde 插件可以配置用户信息的目录，默认配置在 ～ 目录下；可以存放 skills 等等需要持久化的信息"
    },
    {
      "id": "evo_v11_005",
      "type": "validate",
      "title": "全量验收：构建通过、目录自动创建、Skills 文件持久化、配置可变更",
      "status": "done",
      "depends_on": [
        "evo_v11_004"
      ],
      "acceptance": "VSCode 插件 compile 通过；user-data-manager.ts 存在且导出 UserDataManager 类；extension.ts 集成 UserDataManager；SkillRegistry 使用文件持久化；package.json 有 userDataDir 配置；有 openUserDataDir 命令；ConnectionTree 显示数据目录状态",
      "acceptance_cmd": "cd packages/vscode-ext && npm run compile 2>&1 | tail -3 && test -f src/user-data-manager.ts && grep -q 'class UserDataManager' src/user-data-manager.ts && grep -q 'userDataDir' package.json && grep -q 'UserDataManager' src/extension.ts && grep -q 'UserDataManager' src/skill-registry.ts && grep -q 'openUserDataDir' src/command-registry.ts && echo PASS || echo FAIL",
      "evolution_goal": "vsocde 插件可以配置用户信息的目录，默认配置在 ～ 目录下；可以存放 skills 等等需要持久化的信息"
    },
    {
      "id": "evo_v12_001",
      "type": "coding",
      "title": "全局错误兜底 + 服务初始化健康检查：extension.ts activate() 中增加 process uncaughtException/unhandledRejection 处理器，服务初始化失败时标记不健康并阻断下游依赖",
      "status": "done",
      "depends_on": [
        "evo_v11_005"
      ],
      "acceptance": "extension.ts 注册 process-level 错误处理器；wsServer.start() 失败时 MessageHandler 不注册消息回调；outputChannel 记录初始化失败并显示用户可见错误通知；编译通过无 TS 错误",
      "acceptance_cmd": "cd packages/vscode-ext && npm run compile 2>&1 | tail -3 && grep -c \"uncaughtException\\|unhandledRejection\\|process\\.on\" src/extension.ts && grep -c \"健康\\|healthy\\|initialized\" src/extension.ts && echo PASS || echo FAIL",
      "evolution_goal": "vscode 插件容易崩溃；分析优化"
    },
    {
      "id": "evo_v12_002",
      "type": "coding",
      "title": "CancellationToken 生命周期修复：message-handler activeChatTokens WebSocket 断开自动清理 + agent-loop/lm-service 孤立 CTS 修复",
      "status": "done",
      "depends_on": [
        "evo_v12_001"
      ],
      "acceptance": "MessageHandler 监听 WebSocket close 事件，断开时自动 cancel+dispose 对应 CTS 并从 Map 中删除；同一 ws 快速重发 chat 时先 dispose 旧 CTS；agent-loop callLlm 不再创建孤立 CTS；lm-service 同理；编译通过",
      "acceptance_cmd": "cd packages/vscode-ext && npm run compile 2>&1 | tail -3 && grep -c \"ws\\.on.*close\\|onClose\\|addEventListener.*close\" src/message-handler.ts && grep -v \"new vscode.CancellationTokenSource().token\" src/agent-loop.ts > /dev/null && echo PASS || echo FAIL",
      "evolution_goal": "vscode 插件容易崩溃；分析优化"
    },
    {
      "id": "evo_v12_003",
      "type": "coding",
      "title": "内存管理优化：LlmRequestCollector 添加环形缓冲淘汰策略（最多保留 50 条）+ activeChatTokens/pendingRequests disposal guard + ConnectionTree 异步回调 disposed 检查",
      "status": "done",
      "depends_on": [
        "evo_v12_002"
      ],
      "acceptance": "LlmRequestCollector 有 MAX_ENTRIES 常量限制存储数量；pendingRequests 在 dispose 后拒绝新增；ConnectionTree 异步回调检查 disposed 标志后再 refresh；编译通过",
      "acceptance_cmd": "cd packages/vscode-ext && npm run compile 2>&1 | tail -3 && grep -c \"MAX_ENTRIES\\|maxEntries\\|MAX_SIZE\" src/llm-request-collector.ts && grep -c \"disposed\\|_disposed\\|isDisposed\" src/connection-tree.ts && echo PASS || echo FAIL",
      "evolution_goal": "vscode 插件容易崩溃；分析优化"
    },
    {
      "id": "evo_v12_004",
      "type": "coding",
      "title": "WebSocket 心跳 + AgentLoop 超时保护：ws-server 增加 ping/pong 心跳检测（30s 间隔）自动清理死连接 + AgentLoop.run() 增加总超时（5 分钟）防止无限挂起",
      "status": "done",
      "depends_on": [
        "evo_v12_003"
      ],
      "acceptance": "WsServer 有 heartbeat interval 定时 ping 所有客户端并检测 pong 超时断开；dispose 时清除 interval；AgentLoop.run() 有 totalTimeout 参数，超时自动中断并返回 fallback 答案；编译通过",
      "acceptance_cmd": "cd packages/vscode-ext && npm run compile 2>&1 | tail -3 && grep -c \"heartbeat\\|ping.*pong\\|setInterval\" src/ws-server.ts && grep -c \"totalTimeout\\|TOTAL_TIMEOUT\\|AbortSignal\\|setTimeout\" src/agent-loop.ts && echo PASS || echo FAIL",
      "evolution_goal": "vscode 插件容易崩溃；分析优化"
    },
    {
      "id": "evo_v12_005",
      "type": "validation",
      "title": "稳定性优化全量验收：双端构建通过 + 服务初始化失败场景不崩溃 + 无 TS 编译错误 + 核心防御性代码全部就位",
      "status": "done",
      "depends_on": [
        "evo_v12_004"
      ],
      "acceptance": "pnpm build 全量通过；extension.ts 有错误兜底；message-handler 有 ws close 清理；LlmRequestCollector 有淘汰策略；ws-server 有心跳；agent-loop 有超时保护；所有 CTS 无泄漏",
      "acceptance_cmd": "cd packages/vscode-ext && npm run compile 2>&1 | tail -3 && grep -l \"uncaughtException\" src/extension.ts > /dev/null && grep -l \"heartbeat\" src/ws-server.ts > /dev/null && grep -l \"MAX_ENTRIES\\|maxEntries\" src/llm-request-collector.ts > /dev/null && grep -l \"totalTimeout\\|TOTAL_TIMEOUT\" src/agent-loop.ts > /dev/null && echo PASS || echo FAIL",
      "evolution_goal": "vscode 插件容易崩溃；分析优化"
    },
    {
      "id": "evo_v13_001",
      "type": "coding",
      "title": "修复 App.tsx useEffect 无限循环：connectionDetails 依赖导致 list_models 死循环",
      "status": "done",
      "depends_on": [
        "evo_v12_005"
      ],
      "acceptance": "App.tsx 中连接状态变化 useEffect 不再以 connectionDetails 或 debugLog 为依赖；使用 ref 追踪上一次 connectionState，仅在状态从非 connected 变为 connected 时才发送 list_models；构建通过无 TS 错误",
      "acceptance_cmd": "bash -c 'cd packages/chrome-ext && grep -c \"useRef.*connectionState\\|prevConnectionState\\|prevStateRef\" entrypoints/sidepanel/App.tsx && ! grep \"connectionDetails\" entrypoints/sidepanel/App.tsx | grep -q \"useEffect\" && npm run build 2>&1 | grep -i \"error\" | wc -l | xargs -I{} test {} -eq 0 && echo PASS || echo FAIL'",
      "evolution_goal": "修复消息日志一直出现list_models 导致 vsocde 卡死"
    },
    {
      "id": "evo_v13_002",
      "type": "coding",
      "title": "VSCode 侧 handleListModels 节流防护：最小间隔 5 秒，防止高频 list_models 请求压垮 vscode.lm API",
      "status": "done",
      "depends_on": [
        "evo_v13_001"
      ],
      "acceptance": "MessageHandler.handleListModels 内部维护 lastListModelsTime 时间戳，5 秒内重复请求直接返回缓存结果或忽略；outputChannel 记录节流日志；编译通过",
      "acceptance_cmd": "cd packages/vscode-ext && grep -c 'lastListModels\\|LIST_MODELS_THROTTLE\\|throttle\\|_lastListModels' src/message-handler.ts && npm run compile 2>&1 | grep -c 'error' | xargs -I{} test {} -eq 0 && echo PASS || echo FAIL",
      "evolution_goal": "修复消息日志一直出现list_models 导致 vsocde 卡死"
    },
    {
      "id": "evo_v13_003",
      "type": "coding",
      "title": "useWebSocket connectionDetails 更新优化：浅比较减少不必要的 React 状态更新和重渲染",
      "status": "done",
      "depends_on": [
        "evo_v13_001"
      ],
      "acceptance": "useWebSocket.ts 中 onMessage 回调不再每条消息都 setConnectionDetails；改为浅比较（reconnectCount/latency/state 实际变化时才 setState）或使用 useRef + 定时同步；构建通过",
      "acceptance_cmd": "cd packages/chrome-ext && grep -c 'shallowEqual\\|prevDetails\\|detailsRef\\|===.*reconnectCount\\|===.*latency' hooks/useWebSocket.ts && npm run build 2>&1 | grep -i 'error' | wc -l | xargs -I{} test {} -eq 0 && echo PASS || echo FAIL",
      "evolution_goal": "修复消息日志一直出现list_models 导致 vsocde 卡死"
    },
    {
      "id": "evo_v13_004",
      "type": "coding",
      "title": "message-tree.ts TreeView 刷新节流：批量聚合 captureMessage 事件，防止高频消息导致 TreeView 疯狂刷新拖慢 VSCode",
      "status": "done",
      "depends_on": [
        "evo_v13_002"
      ],
      "acceptance": "MessageTreeDataProvider 构造函数中对 onDidCaptureMessage 事件添加 debounce/throttle（100-300ms 间隔），不再每条消息都触发 _onDidChangeTreeData.fire()；编译通过",
      "acceptance_cmd": "cd packages/vscode-ext && grep -c 'debounce\\|throttle\\|setTimeout\\|_refreshTimer\\|batchRefresh' src/message-tree.ts && npm run compile 2>&1 | grep -c 'error' | xargs -I{} test {} -eq 0 && echo PASS || echo FAIL",
      "evolution_goal": "修复消息日志一直出现list_models 导致 vsocde 卡死"
    },
    {
      "id": "evo_v13_005",
      "type": "validation",
      "title": "list_models 死循环修复全量验收：双端构建通过 + list_models 仅连接时发送一次 + 无 TreeView 抖动 + 无 outputChannel 洪水",
      "status": "done",
      "depends_on": [
        "evo_v13_003",
        "evo_v13_004"
      ],
      "acceptance": "pnpm build 全量通过；App.tsx 使用 ref 追踪 connectionState 转换且不依赖 connectionDetails；MessageHandler 有 list_models 节流；useWebSocket 有 connectionDetails 浅比较；MessageTreeDataProvider 有刷新节流；整体无 TS 编译错误",
      "acceptance_cmd": "cd packages/chrome-ext && npm run build 2>&1 | grep -i 'error' | wc -l | xargs -I{} test {} -eq 0 && cd ../../packages/vscode-ext && npm run compile 2>&1 | grep -c 'error' | xargs -I{} test {} -eq 0 && echo PASS || echo FAIL",
      "evolution_goal": "修复消息日志一直出现list_models 导致 vsocde 卡死"
    },
    {
      "id": "evo_v14_001",
      "type": "coding",
      "title": "修复 App.tsx useEffect(onMessage) 依赖：移除 debugLog，改用 debugLogRef.current 读取日志方法",
      "status": "done",
      "depends_on": [
        "evo_v13_005"
      ],
      "acceptance": "App.tsx 第一个 useEffect（onMessage 消息注册）的依赖数组不包含 debugLog；effect 内部通过 debugLogRef.current 调用 logInbound/logExecution/startTimeline 等方法；Chrome 插件构建通过",
      "acceptance_cmd": "cd packages/chrome-ext && grep -A2 '\\[onMessage' entrypoints/sidepanel/App.tsx | grep -v 'debugLog[^R]' && npm run build 2>&1 | grep -i 'error' | wc -l | xargs -I{} test {} -eq 0 && echo PASS || echo FAIL",
      "evolution_goal": "修复 debug 持续显示 [render] Should have a queue. This is likely a bug in React. Please file an issue."
    },
    {
      "id": "evo_v14_002",
      "type": "coding",
      "title": "修复 App.tsx errorLog 同步 useEffect 依赖：移除 debugLog，改用 debugLogRef.current.logError",
      "status": "done",
      "depends_on": [
        "evo_v14_001"
      ],
      "acceptance": "App.tsx 中 errorLog 同步 useEffect 的依赖数组仅包含 [errorLog]，不包含 debugLog；effect 内部通过 debugLogRef.current.logError 调用；构建通过",
      "acceptance_cmd": "cd packages/chrome-ext && grep -A2 '\\[errorLog' entrypoints/sidepanel/App.tsx | grep -v 'debugLog[^R]' && npm run build 2>&1 | grep -i 'error' | wc -l | xargs -I{} test {} -eq 0 && echo PASS || echo FAIL",
      "evolution_goal": "修复 debug 持续显示 [render] Should have a queue. This is likely a bug in React. Please file an issue."
    },
    {
      "id": "evo_v14_003",
      "type": "coding",
      "title": "修复 App.tsx handleModelSelect/handleSendMessage useCallback 依赖：移除 debugLog，改用 debugLogRef.current.logOutbound",
      "status": "done",
      "depends_on": [
        "evo_v14_001"
      ],
      "acceptance": "handleModelSelect 和 handleSendMessage 两个 useCallback 的依赖数组不包含 debugLog；回调内部通过 debugLogRef.current.logOutbound 调用；构建通过",
      "acceptance_cmd": "cd packages/chrome-ext && grep -c 'debugLogRef.current.logOutbound' entrypoints/sidepanel/App.tsx | xargs -I{} test {} -ge 2 && grep 'sendMessage, debugLog\\]\\|chatSend.*debugLog\\]' entrypoints/sidepanel/App.tsx | wc -l | xargs -I{} test {} -eq 0 && npm run build 2>&1 | grep -i 'error' | wc -l | xargs -I{} test {} -eq 0 && echo PASS || echo FAIL",
      "evolution_goal": "修复 debug 持续显示 [render] Should have a queue. This is likely a bug in React. Please file an issue."
    },
    {
      "id": "evo_v14_004",
      "type": "coding",
      "title": "useDebugLog 返回值稳定化：用 useMemo 包裹返回对象，仅在 logs/timeline/toggles/stats 变化时重建",
      "status": "done",
      "depends_on": [
        "evo_v14_003"
      ],
      "acceptance": "useDebugLog.ts 的 return 语句使用 useMemo 包裹，依赖数组为 [logs, timeline, toggles, stats] 及所有 useCallback 函数引用；避免每次渲染返回新对象；构建通过",
      "acceptance_cmd": "cd packages/chrome-ext && grep -c 'useMemo' hooks/useDebugLog.ts | xargs -I{} test {} -ge 1 && npm run build 2>&1 | grep -i 'error' | wc -l | xargs -I{} test {} -eq 0 && echo PASS || echo FAIL",
      "evolution_goal": "修复 debug 持续显示 [render] Should have a queue. This is likely a bug in React. Please file an issue."
    },
    {
      "id": "evo_v14_005",
      "type": "validation",
      "title": "React queue 错误修复全量验收：Chrome 插件构建通过 + App.tsx 无 debugLog 直接依赖 + 无 React queue 警告",
      "status": "done",
      "depends_on": [
        "evo_v14_002",
        "evo_v14_003",
        "evo_v14_004"
      ],
      "acceptance": "pnpm build 全量通过；App.tsx 所有 useEffect/useCallback 依赖数组均不直接包含 debugLog（仅用 debugLogRef）；useDebugLog.ts 返回值使用 useMemo 稳定化；无 TS 编译错误；运行时不再出现 '[render] Should have a queue' 警告",
      "acceptance_cmd": "cd packages/chrome-ext && npm run build 2>&1 | grep -i 'error' | wc -l | xargs -I{} test {} -eq 0 && grep -c 'debugLog\\]' entrypoints/sidepanel/App.tsx | xargs -I{} test {} -eq 0 && cd ../../packages/vscode-ext && npm run compile 2>&1 | grep -c 'error' | xargs -I{} test {} -eq 0 && echo PASS || echo FAIL",
      "evolution_goal": "修复 debug 持续显示 [render] Should have a queue. This is likely a bug in React. Please file an issue."
    },
    {
      "id": "evo_v15_001",
      "type": "coding",
      "title": "WsClient 重连指数退避：固定 3s 改为 1s→2s→4s→8s→max30s + jitter + maxReconnectAttempts=20 + 合并重连日志",
      "status": "done",
      "depends_on": [
        "evo_v14_005"
      ],
      "acceptance": "ws-client.ts scheduleReconnect() 使用指数退避算法（baseInterval * 2^count + random jitter）；有 MAX_RECONNECT_INTERVAL 常量（30s）；maxReconnectAttempts 默认 20（非 Infinity）；connect() 内不再每次都打 console.log，改为首次/每 5 次/最后一次打印；构建通过",
      "acceptance_cmd": "cd packages/chrome-ext && grep -c 'Math.min\\|Math.pow\\|backoff\\|exponential\\|jitter' src/ws-client.ts | xargs -I{} test {} -ge 1 && grep -c 'MAX_RECONNECT\\|maxReconnect.*=.*[0-9]' src/ws-client.ts | xargs -I{} test {} -ge 1 && npm run build 2>&1 | grep -i 'error' | wc -l | xargs -I{} test {} -eq 0 && echo PASS || echo FAIL",
      "evolution_goal": "结合 debug-log-2026-03-21T14-34-26-549Z.json 日志文件优化"
    },
    {
      "id": "evo_v15_002",
      "type": "coding",
      "title": "Navigate 等待页面加载完成：background.ts browser.tabs.update 后监听 onUpdated(status=complete) + 验证 content script 就绪再返回成功",
      "status": "done",
      "depends_on": [
        "evo_v15_001"
      ],
      "acceptance": "background.ts navigate 处理中：tabs.update() 后注册 tabs.onUpdated 监听器等待 status==='complete'；完成后尝试 tabs.sendMessage ping content script 确认就绪；有 15s 超时保护；整个过程完成后才 sendResponse；构建通过",
      "acceptance_cmd": "cd packages/chrome-ext && grep -c 'onUpdated\\|status.*complete\\|waitFor.*load\\|pageLoad' entrypoints/background.ts | xargs -I{} test {} -ge 2 && npm run build 2>&1 | grep -i 'error' | wc -l | xargs -I{} test {} -eq 0 && echo PASS || echo FAIL",
      "evolution_goal": "结合 debug-log-2026-03-21T14-34-26-549Z.json 日志文件优化"
    },
    {
      "id": "evo_v15_003",
      "type": "coding",
      "title": "工具参数自动修正：tool-bridge.ts toAction 增加 type 操作 text→value 兼容映射 + browser-tools.ts 优化 browser_type 描述防止 LLM 混淆",
      "status": "done",
      "depends_on": [
        "evo_v15_001"
      ],
      "acceptance": "tool-bridge.ts toAction() 针对 toolName==='type' 时如果有 text 无 value 则自动映射 text→value；browser-tools.ts browser_type 描述中明确强调 'Use the `value` parameter (NOT `text`)'；构建通过",
      "acceptance_cmd": "cd packages/chrome-ext && grep -c 'text.*value\\|value.*text\\|toolArgs.*text' utils/tool-bridge.ts | xargs -I{} test {} -ge 1 && npm run build 2>&1 | grep -i 'error' | wc -l | xargs -I{} test {} -eq 0 && cd ../../packages/vscode-ext && grep -i 'not.*text\\|value.*parameter' src/browser-tools.ts && npm run compile 2>&1 | grep -c 'error' | xargs -I{} test {} -eq 0 && echo PASS || echo FAIL",
      "evolution_goal": "结合 debug-log-2026-03-21T14-34-26-549Z.json 日志文件优化"
    },
    {
      "id": "evo_v15_004",
      "type": "coding",
      "title": "心跳容错优化：ws-client.ts 收到任何业务消息时重置 pong 超时计时器，避免 Agent 执行期间误判断连",
      "status": "done",
      "depends_on": [
        "evo_v15_001"
      ],
      "acceptance": "ws-client.ts onmessage 回调中，收到任何有效 BridgeMessage 时都执行 resetPongTimeout()（清除并重新启动 pong 超时定时器）；不仅仅响应 heartbeat_pong/pong；确保 Agent 执行期间有消息流动就不会触发心跳超时断连；构建通过",
      "acceptance_cmd": "cd packages/chrome-ext && grep -c 'resetPongTimeout\\|clearPongTimeout\\|restartPongTimer\\|pongTimeoutTimer' src/ws-client.ts | xargs -I{} test {} -ge 3 && npm run build 2>&1 | grep -i 'error' | wc -l | xargs -I{} test {} -eq 0 && echo PASS || echo FAIL",
      "evolution_goal": "结合 debug-log-2026-03-21T14-34-26-549Z.json 日志文件优化"
    },
    {
      "id": "evo_v15_005",
      "type": "validation",
      "title": "Debug 日志优化全量验收：双端构建通过 + 重连指数退避 + 导航等待加载 + 工具参数兼容 + 心跳容错",
      "status": "done",
      "depends_on": [
        "evo_v15_002",
        "evo_v15_003",
        "evo_v15_004"
      ],
      "acceptance": "pnpm build 全量通过；ws-client.ts 有指数退避+jitter+maxReconnect 上限；background.ts navigate 等待 page load complete；tool-bridge.ts 有 text→value 兼容映射；ws-client.ts 业务消息重置心跳超时；所有 TS 编译无错误",
      "acceptance_cmd": "cd packages/chrome-ext && npm run build 2>&1 | grep -i 'error' | wc -l | xargs -I{} test {} -eq 0 && grep -c 'backoff\\|exponential\\|Math.min' src/ws-client.ts | xargs -I{} test {} -ge 1 && grep -c 'onUpdated\\|status.*complete' entrypoints/background.ts | xargs -I{} test {} -ge 2 && grep -c 'text.*value' utils/tool-bridge.ts | xargs -I{} test {} -ge 1 && cd ../../packages/vscode-ext && npm run compile 2>&1 | grep -c 'error' | xargs -I{} test {} -eq 0 && echo PASS || echo FAIL",
      "evolution_goal": "结合 debug-log-2026-03-21T14-34-26-549Z.json 日志文件优化"
    },
    {
      "id": "evo_v16_001",
      "type": "coding",
      "title": "Server 单客户端模式：Set<WebSocket> → activeClient 单引用 + 新连接踢旧连接",
      "status": "done",
      "depends_on": [
        "evo_v15_005"
      ],
      "acceptance": "ws-server.ts 中 clients: Set<WebSocket> 替换为 activeClient: WebSocket | null；新连接到达时若 activeClient 存在且 OPEN/CONNECTING 则 close(4001)；clientAliveMap 替换为 isClientAlive: boolean；firstClient getter 直接返回 activeClient；clientCount 返回 0 或 1；broadcast() 简化为发送到 activeClient；heartbeat 只检测 activeClient；编译通过",
      "acceptance_cmd": "cd packages/vscode-ext && grep -c 'activeClient' src/ws-server.ts | xargs -I{} test {} -ge 3 && grep -c 'Set<WebSocket>' src/ws-server.ts | xargs -I{} test {} -eq 0 && grep -c '4001\\|replaced' src/ws-server.ts | xargs -I{} test {} -ge 1 && npm run compile 2>&1 | grep -c 'error' | xargs -I{} test {} -eq 0 && echo PASS || echo FAIL",
      "evolution_goal": "考虑只有唯一客户端，减少没必要的重连"
    },
    {
      "id": "evo_v16_002",
      "type": "coding",
      "title": "Client 连接去重保护：connect() 增加 OPEN/CONNECTING 状态跳过 + 重连次数降至 10",
      "status": "done",
      "depends_on": [
        "evo_v15_005"
      ],
      "acceptance": "ws-client.ts connect() 方法开头检查 ws.readyState === OPEN 或 CONNECTING 时直接 return（防重入）；DEFAULT_MAX_RECONNECT_ATTEMPTS 从 20 降为 10；jitter 注释不再提及多客户端；构建通过",
      "acceptance_cmd": "cd packages/chrome-ext && grep -c 'readyState.*OPEN\\|readyState.*CONNECTING\\|already.*connect' src/ws-client.ts | xargs -I{} test {} -ge 1 && grep 'DEFAULT_MAX_RECONNECT_ATTEMPTS.*=.*10' src/ws-client.ts && npm run build 2>&1 | grep -i 'error' | wc -l | xargs -I{} test {} -eq 0 && echo PASS || echo FAIL",
      "evolution_goal": "考虑只有唯一客户端，减少没必要的重连"
    },
    {
      "id": "evo_v16_003",
      "type": "coding",
      "title": "Client 可见性感知重连：Side Panel 隐藏时暂停重连，可见时立即恢复",
      "status": "done",
      "depends_on": [
        "evo_v16_002"
      ],
      "acceptance": "ws-client.ts 监听 document.visibilitychange 事件；hidden 时清除 reconnectTimer 并设 _pausedByVisibility=true；visible 时若非 connected 则 reconnectCount=0 并立即 connect()；dispose 时移除监听器；构建通过",
      "acceptance_cmd": "cd packages/chrome-ext && grep -c 'visibilitychange\\|visibilityState\\|hidden\\|visible\\|_pausedByVisibility\\|pausedByVisibility' src/ws-client.ts | xargs -I{} test {} -ge 3 && npm run build 2>&1 | grep -i 'error' | wc -l | xargs -I{} test {} -eq 0 && echo PASS || echo FAIL",
      "evolution_goal": "考虑只有唯一客户端，减少没必要的重连"
    },
    {
      "id": "evo_v16_004",
      "type": "coding",
      "title": "Server welcome 握手：新连接发送 welcome 消息 + 记录 sessionId + Client 处理 welcome",
      "status": "done",
      "depends_on": [
        "evo_v16_001"
      ],
      "acceptance": "ws-server.ts 在 connection 事件中发送 { type: 'welcome', payload: { replacedPrevious } }；outputChannel 记录连接客户端的 sessionId；ws-client.ts 在 onmessage 中处理 welcome 消息并 console.log；双端编译通过",
      "acceptance_cmd": "cd packages/vscode-ext && grep -c 'welcome' src/ws-server.ts | xargs -I{} test {} -ge 1 && npm run compile 2>&1 | grep -c 'error' | xargs -I{} test {} -eq 0 && cd ../../packages/chrome-ext && grep -c 'welcome' src/ws-client.ts | xargs -I{} test {} -ge 1 && npm run build 2>&1 | grep -i 'error' | wc -l | xargs -I{} test {} -eq 0 && echo PASS || echo FAIL",
      "evolution_goal": "考虑只有唯一客户端，减少没必要的重连"
    },
    {
      "id": "evo_v16_005",
      "type": "validation",
      "title": "单客户端优化全量验收：双端构建通过 + activeClient 单引用 + 连接去重 + 可见性感知 + welcome 握手",
      "status": "done",
      "depends_on": [
        "evo_v16_003",
        "evo_v16_004"
      ],
      "acceptance": "pnpm 全量构建通过；ws-server.ts 使用 activeClient（非 Set）+ 新连接踢旧 + welcome 握手；ws-client.ts 连接去重 + maxAttempts=10 + visibilitychange 暂停/恢复；无 TS 编译错误",
      "acceptance_cmd": "cd packages/vscode-ext && grep -c 'activeClient' src/ws-server.ts | xargs -I{} test {} -ge 3 && grep -c 'welcome' src/ws-server.ts | xargs -I{} test {} -ge 1 && npm run compile 2>&1 | grep -c 'error' | xargs -I{} test {} -eq 0 && cd ../../packages/chrome-ext && grep -c 'visibilitychange' src/ws-client.ts | xargs -I{} test {} -ge 1 && grep 'DEFAULT_MAX_RECONNECT_ATTEMPTS.*=.*10' src/ws-client.ts && npm run build 2>&1 | grep -i 'error' | wc -l | xargs -I{} test {} -eq 0 && echo PASS || echo FAIL",
      "evolution_goal": "考虑只有唯一客户端，减少没必要的重连"
    },
    {
      "id": "evo_v17_001",
      "type": "coding",
      "title": "Context Budget 常量与截断工具模块：vscode-ext/src/context-budget.ts",
      "status": "done",
      "depends_on": [
        "evo_v16_005"
      ],
      "acceptance": "新建 context-budget.ts，导出 MAX_SELECTED_TEXT_CHARS(8000)、MAX_URL_CHARS(2000)、MAX_TITLE_CHARS(500)、MAX_SYSTEM_PROMPT_CONTEXT_CHARS(12000)、MAX_OBSERVATION_CHARS(6000)、MAX_MESSAGES_CHARS(80000) 常量；导出 estimateTokens(text)、smartTruncate(text, maxChars, options?) 函数；smartTruncate 保留头尾并插入 ...[已截断 N 字符]... 占位符；编译通过",
      "acceptance_cmd": "cd packages/vscode-ext && grep -q 'MAX_SELECTED_TEXT_CHARS' src/context-budget.ts && grep -q 'smartTruncate' src/context-budget.ts && grep -q 'estimateTokens' src/context-budget.ts && npm run compile 2>&1 | tail -1 && echo PASS || echo FAIL",
      "evolution_goal": "不考虑时间，使用最优解，处理浏览器上下文爆炸的问题"
    },
    {
      "id": "evo_v17_002",
      "type": "coding",
      "title": "Chrome 侧上下文采集预截断：content.ts selectedText 限制 + usePageContext 防护",
      "status": "done",
      "depends_on": [
        "evo_v17_001"
      ],
      "acceptance": "content.ts 中 selectedText 在采集时截断到 MAX_SELECTED_TEXT_CHARS(8000 字符)；selectionchange 事件同样限制；usePageContext hook 对 url/title/selectedText 做二次防护截断；Chrome 插件构建通过",
      "acceptance_cmd": "cd packages/chrome-ext && grep -q '8000\\|MAX_SELECTED' entrypoints/content.ts && grep -q 'substring\\|slice\\|truncat' entrypoints/content.ts && npm run build 2>&1 | tail -3 && echo PASS || echo FAIL",
      "evolution_goal": "不考虑时间，使用最优解，处理浏览器上下文爆炸的问题"
    },
    {
      "id": "evo_v17_003",
      "type": "coding",
      "title": "VSCode 侧 System Prompt 上下文预算：buildSystemPrompt 截断 + 日志监控",
      "status": "done",
      "depends_on": [
        "evo_v17_001"
      ],
      "acceptance": "message-handler.ts 的 buildSystemPrompt() 使用 smartTruncate 对 selectedText 截断；总上下文部分不超过 MAX_SYSTEM_PROMPT_CONTEXT_CHARS；日志输出上下文字符数和 token 估算值；编译通过",
      "acceptance_cmd": "cd packages/vscode-ext && grep -q 'smartTruncate\\|truncat' src/message-handler.ts && grep -q 'MAX_SYSTEM_PROMPT_CONTEXT_CHARS\\|contextBudget\\|12000' src/message-handler.ts && npm run compile 2>&1 | tail -1 && echo PASS || echo FAIL",
      "evolution_goal": "不考虑时间，使用最优解，处理浏览器上下文爆炸的问题"
    },
    {
      "id": "evo_v17_004",
      "type": "coding",
      "title": "AgentLoop 观察结果截断 + 消息窗口管理：防止 ReAct 循环 token 溢出",
      "status": "done",
      "depends_on": [
        "evo_v17_001"
      ],
      "acceptance": "agent-loop.ts 中 OBSERVATION 结果在添加到 messages 前通过 smartTruncate 截断到 MAX_OBSERVATION_CHARS；新增 trimMessages() 方法，当 messages 总字符数超过 MAX_MESSAGES_CHARS 时移除最早的非系统消息轮次（保留 system prompt + 最近 N 轮）；日志记录截断事件；编译通过",
      "acceptance_cmd": "cd packages/vscode-ext && grep -q 'MAX_OBSERVATION_CHARS\\|truncat.*observation\\|trimMessages' src/agent-loop.ts && grep -q 'MAX_MESSAGES_CHARS\\|messageBudget\\|80000' src/agent-loop.ts && npm run compile 2>&1 | tail -1 && echo PASS || echo FAIL",
      "evolution_goal": "不考虑时间，使用最优解，处理浏览器上下文爆炸的问题"
    },
    {
      "id": "evo_v17_005",
      "type": "validation",
      "title": "上下文预算体系全量验收：双端构建 + 截断常量存在 + 关键路径覆盖",
      "status": "done",
      "depends_on": [
        "evo_v17_002",
        "evo_v17_003",
        "evo_v17_004"
      ],
      "acceptance": "Chrome 插件构建通过 + VSCode 插件编译通过；context-budget.ts 存在且导出所有常量和工具函数；content.ts 有截断逻辑；buildSystemPrompt 有预算控制；agent-loop.ts 有观察截断和消息窗口管理",
      "acceptance_cmd": "cd packages/chrome-ext && npm run build 2>&1 | tail -1 && cd ../../packages/vscode-ext && npm run compile 2>&1 | tail -1 && test -f src/context-budget.ts && grep -q 'smartTruncate' src/context-budget.ts && grep -q 'truncat\\|slice\\|8000' ../chrome-ext/entrypoints/content.ts && grep -q 'smartTruncate\\|MAX_SYSTEM' src/message-handler.ts && grep -q 'trimMessages\\|MAX_OBSERVATION' src/agent-loop.ts && echo PASS || echo FAIL",
      "evolution_goal": "不考虑时间，使用最优解，处理浏览器上下文爆炸的问题"
    },
    {
      "id": "evo_v18_001",
      "type": "coding",
      "title": "扩展核心浏览器工具集：browser-tools.ts 新增 5 个工具 + action-executor.ts 新增 3 个 ActionType",
      "status": "done",
      "depends_on": [
        "evo_v17_005"
      ],
      "acceptance": "browser-tools.ts 新增 browser_query_selector_all / browser_get_value / browser_evaluate / browser_select_option / browser_get_links 共 5 个工具定义和 TOOL_MAPPING；action-executor.ts 新增 evaluate / selectOption / getLinks 三种 ActionType 实现；双端编译通过",
      "acceptance_cmd": "cd packages/vscode-ext && grep -c 'browser_evaluate\\|browser_select_option\\|browser_get_links\\|browser_query_selector_all\\|browser_get_value' src/browser-tools.ts | xargs -I{} test {} -ge 10 && npm run compile 2>&1 | tail -1 && cd ../../packages/chrome-ext && grep -c 'evaluate\\|selectOption\\|getLinks' utils/action-executor.ts | xargs -I{} test {} -ge 6 && npm run build 2>&1 | tail -1 && echo PASS || echo FAIL",
      "evolution_goal": "不考虑时间，使用最优解，想优化内置 skill，提供尽可能多有价值的 skill，用户能再让 agent 执行多步骤任务，如果需要可以补充核心工具"
    },
    {
      "id": "evo_v18_002",
      "type": "coding",
      "title": "SkillRunner 步骤结果传递增强：支持 {{$prev}} 和 {{$step_N}} 插值语法",
      "status": "done",
      "depends_on": [
        "evo_v18_001"
      ],
      "acceptance": "SkillRunner.execute() 在逐步执行时收集 stepResults，interpolateValue() 支持 {{$prev}}（上一步结果文本）和 {{$step_0}} / {{$step_1}} / ...（指定步骤结果文本）占位符替换；VSCode 插件编译通过",
      "acceptance_cmd": "cd packages/vscode-ext && grep -q '\\$prev\\|\\$step_' src/skill-runner.ts && grep -q 'stepResults\\|previousResult\\|resultMap' src/skill-runner.ts && npm run compile 2>&1 | tail -1 && echo PASS || echo FAIL",
      "evolution_goal": "不考虑时间，使用最优解，想优化内置 skill，提供尽可能多有价值的 skill，用户能再让 agent 执行多步骤任务，如果需要可以补充核心工具"
    },
    {
      "id": "evo_v18_003",
      "type": "coding",
      "title": "新增 10 个高价值内置预设 Skill：read_article / capture_all_links / capture_table_data / search_in_page / auto_scroll_read / open_and_summarize / fill_and_submit / page_info / multi_step_extract / batch_screenshot",
      "status": "done",
      "depends_on": [
        "evo_v18_002"
      ],
      "acceptance": "skill-registry.ts 的 PRESET_SKILLS 数组包含至少 15 个预设 Skill（原 5 + 新 10）；每个 Skill 有完整的 name / displayName / description / steps 定义；VSCode 插件编译通过",
      "acceptance_cmd": "cd packages/vscode-ext && node -e \"const fs=require('fs'); const c=fs.readFileSync('src/skill-registry.ts','utf8'); const m=c.match(/name:\\s*'[\\w_]+'/g); console.log('Preset count:', m?m.length:0); process.exit(m&&m.length>=15?0:1)\" && npm run compile 2>&1 | tail -1 && echo PASS || echo FAIL",
      "evolution_goal": "不考虑时间，使用最优解，想优化内置 skill，提供尽可能多有价值的 skill，用户能再让 agent 执行多步骤任务，如果需要可以补充核心工具"
    },
    {
      "id": "evo_v18_004",
      "type": "coding",
      "title": "AgentLoop 复杂任务编排增强：优化 system prompt 加入工具组合 few-shot 范例 + Skill 编排建议",
      "status": "done",
      "depends_on": [
        "evo_v18_003"
      ],
      "acceptance": "agent-loop.ts 的 buildAgentSystemPrompt 中包含工具组合使用范例（至少 2 个 few-shot example）、多步骤任务编排建议、错误恢复指导；VSCode 插件编译通过",
      "acceptance_cmd": "cd packages/vscode-ext && grep -c 'Example\\|example\\|示例\\|few.shot\\|multi.step\\|EXAMPLE' src/agent-loop.ts | xargs -I{} test {} -ge 3 && grep -q 'run_skill\\|browser_' src/agent-loop.ts && npm run compile 2>&1 | tail -1 && echo PASS || echo FAIL",
      "evolution_goal": "不考虑时间，使用最优解，想优化内置 skill，提供尽可能多有价值的 skill，用户能再让 agent 执行多步骤任务，如果需要可以补充核心工具"
    },
    {
      "id": "evo_v18_005",
      "type": "validation",
      "title": "Skill 体系增强全量验收：双端构建 + 15 个预设 Skill + 15 个浏览器工具 + 步骤传参 + few-shot prompt",
      "status": "done",
      "depends_on": [
        "evo_v18_001",
        "evo_v18_002",
        "evo_v18_003",
        "evo_v18_004"
      ],
      "acceptance": "Chrome 插件构建通过 + VSCode 插件编译通过；browser-tools.ts 有 15 个工具定义；skill-registry.ts 有 15 个预设 Skill；skill-runner.ts 支持 $prev/$step_N 插值；agent-loop.ts 有 few-shot 范例",
      "acceptance_cmd": "cd packages/chrome-ext && npm run build 2>&1 | tail -1 && cd ../../packages/vscode-ext && npm run compile 2>&1 | tail -1 && node -e \"const fs=require('fs'); const b=fs.readFileSync('src/browser-tools.ts','utf8'); const s=fs.readFileSync('src/skill-registry.ts','utf8'); const r=fs.readFileSync('src/skill-runner.ts','utf8'); const a=fs.readFileSync('src/agent-loop.ts','utf8'); const tools=(b.match(/name:\\s*'browser_/g)||[]).length; const skills=(s.match(/name:\\s*'[\\w_]+'/g)||[]).length; const hasInterp=r.includes('\\$prev')||r.includes('\\$step_'); const hasFewShot=a.includes('Example')||a.includes('example'); console.log('Tools:',tools,'Skills:',skills,'Interp:',hasInterp,'FewShot:',hasFewShot); process.exit(tools>=15&&skills>=15&&hasInterp&&hasFewShot?0:1)\" && echo PASS || echo FAIL",
      "evolution_goal": "不考虑时间，使用最优解，想优化内置 skill，提供尽可能多有价值的 skill，用户能再让 agent 执行多步骤任务，如果需要可以补充核心工具"
    },
    {
      "id": "evo_v19_001",
      "type": "coding",
      "title": "沉浸式翻译浏览器工具：browser_extract_paragraphs + browser_inject_bilingual",
      "status": "done",
      "depends_on": [
        "evo_v18_005"
      ],
      "acceptance": "browser-tools.ts 新增 browser_extract_paragraphs（智能内容区域检测 article>main>.content>body，提取 p/h1-h6/li/blockquote/td/figcaption 等段落，返回 [{index, tagName, text, selector}] 数组，每个元素标记 data-imt-id）和 browser_inject_bilingual（接收 [{index, translatedText}] 数组 + mode 参数(inject/toggle/clear)，inject 模式在每个原文段落后插入 .imt-translation 双语 div，toggle 模式切换 .imt-hidden，clear 模式移除所有注入元素）两个工具定义 + TOOL_MAPPINGS；action-executor.ts 新增 extractParagraphs 和 injectBilingual 两个执行函数，含沉浸式翻译 CSS 样式注入；content.ts / background.ts 路由新 ActionType；双端编译通过",
      "acceptance_cmd": "cd packages/vscode-ext && grep -q 'browser_extract_paragraphs' src/browser-tools.ts && grep -q 'browser_inject_bilingual' src/browser-tools.ts && npm run compile 2>&1 | tail -1 && cd ../../packages/chrome-ext && grep -q 'extractParagraphs' utils/action-executor.ts && grep -q 'injectBilingual' utils/action-executor.ts && grep -q 'imt-translation' utils/action-executor.ts && npm run build 2>&1 | tail -1 && echo PASS || echo FAIL",
      "evolution_goal": "不考虑时间，使用最优解，提供沉浸式翻译的 skill，要参考 chrome 插件沉浸式翻译，提供最好的用户体验"
    },
    {
      "id": "evo_v19_002",
      "type": "coding",
      "title": "SkillRunner LLM 工具路由：新增 llm_translate 批量翻译工具",
      "status": "done",
      "depends_on": [
        "evo_v19_001"
      ],
      "acceptance": "skill-runner.ts 新增第三条工具路由：llm_* 前缀 → LmService（与 browser_* → BrowserToolProvider、其他 → McpClient 并列）；新增 llm_translate 工具实现，接收 {texts: string[], targetLanguage: string, sourceLanguage?: string}，通过 vscode.lm API 批量翻译，支持分批处理（每批最多 20 段，避免上下文溢出），返回 {translations: string[]} 数组；新增 llm-tools.ts 定义 LLM 工具元数据（name, description, inputSchema）；SkillRunner 构造函数接收 LmService 引用；VSCode 插件编译通过",
      "acceptance_cmd": "cd packages/vscode-ext && grep -q 'llm_translate\\|llm_' src/skill-runner.ts && grep -q 'LmService\\|lmService' src/skill-runner.ts && test -f src/llm-tools.ts && grep -q 'llm_translate' src/llm-tools.ts && npm run compile 2>&1 | tail -1 && echo PASS || echo FAIL",
      "evolution_goal": "不考虑时间，使用最优解，提供沉浸式翻译的 skill，要参考 chrome 插件沉浸式翻译，提供最好的用户体验"
    },
    {
      "id": "evo_v19_003",
      "type": "coding",
      "title": "immersive_translate 预设 Skill 定义：替换旧 translate_page，4 步沉浸式翻译流程",
      "status": "done",
      "depends_on": [
        "evo_v19_002"
      ],
      "acceptance": "skill-registry.ts 中旧 translate_page skill 被替换为 immersive_translate（或新增并保留旧版）；新 skill 包含 4 步：Step1 browser_extract_paragraphs（智能提取段落）→ Step2 llm_translate({{$prev}} + targetLanguage) → Step3 browser_inject_bilingual({{$prev}} + mode:inject) → Step4 browser_screenshot(optional)；参数包含 targetLanguage（默认中文）、selector（默认 body）、sourceLanguage（可选）；displayName 为「沉浸式翻译」；description 说明双语对照功能；VSCode 插件编译通过",
      "acceptance_cmd": "cd packages/vscode-ext && grep -q 'immersive_translate' src/skill-registry.ts && grep -q 'browser_extract_paragraphs' src/skill-registry.ts && grep -q 'llm_translate' src/skill-registry.ts && grep -q 'browser_inject_bilingual' src/skill-registry.ts && grep -q 'targetLanguage' src/skill-registry.ts && npm run compile 2>&1 | tail -1 && echo PASS || echo FAIL",
      "evolution_goal": "不考虑时间，使用最优解，提供沉浸式翻译的 skill，要参考 chrome 插件沉浸式翻译，提供最好的用户体验"
    },
    {
      "id": "evo_v19_004",
      "type": "coding",
      "title": "Chrome 侧沉浸式翻译 UI 控制：SkillPanel 翻译快捷入口 + toggle/clear 操作按钮",
      "status": "done",
      "depends_on": [
        "evo_v19_003"
      ],
      "acceptance": "SkillPanel.tsx 或新增 TranslateControl.tsx 组件：immersive_translate skill 卡片有特殊 UI 展示（翻译图标 + 目标语言快选）；翻译完成后展示 toggle 按钮（显示/隐藏翻译）和 clear 按钮（清除翻译）；toggle 按钮调用 browser_inject_bilingual(mode:'toggle')，clear 按钮调用 browser_inject_bilingual(mode:'clear')；翻译状态持久化在组件 state 中（已翻译/未翻译）；Chrome 插件构建通过",
      "acceptance_cmd": "cd packages/chrome-ext && grep -rq 'immersive_translate\\|toggle.*translat\\|TranslateControl' components/ && grep -rq 'toggle\\|clear' components/ && npm run build 2>&1 | tail -1 && echo PASS || echo FAIL",
      "evolution_goal": "不考虑时间，使用最优解，提供沉浸式翻译的 skill，要参考 chrome 插件沉浸式翻译，提供最好的用户体验"
    },
    {
      "id": "evo_v19_005",
      "type": "validation",
      "title": "沉浸式翻译全量验收：双端构建 + 新工具 + LLM 路由 + Skill 定义 + UI 控制",
      "status": "done",
      "depends_on": [
        "evo_v19_001",
        "evo_v19_002",
        "evo_v19_003",
        "evo_v19_004"
      ],
      "acceptance": "Chrome 插件构建通过 + VSCode 插件编译通过；browser-tools.ts 有 browser_extract_paragraphs 和 browser_inject_bilingual 工具定义；action-executor.ts 有 extractParagraphs 和 injectBilingual 实现（含 .imt-translation CSS）；skill-runner.ts 有 llm_* 路由 + llm_translate 实现；skill-registry.ts 有 immersive_translate Skill（4 步流程）；Chrome 侧有翻译 toggle/clear UI 控制",
      "acceptance_cmd": "cd packages/chrome-ext && npm run build 2>&1 | tail -1 && cd ../../packages/vscode-ext && npm run compile 2>&1 | tail -1 && grep -q 'browser_extract_paragraphs' src/browser-tools.ts && grep -q 'browser_inject_bilingual' src/browser-tools.ts && grep -q 'extractParagraphs' ../chrome-ext/utils/action-executor.ts && grep -q 'injectBilingual' ../chrome-ext/utils/action-executor.ts && grep -q 'imt-translation' ../chrome-ext/utils/action-executor.ts && grep -q 'llm_translate' src/skill-runner.ts && grep -q 'immersive_translate' src/skill-registry.ts && grep -rq 'toggle\\|clear' ../chrome-ext/components/ && echo PASS || echo FAIL",
      "evolution_goal": "不考虑时间，使用最优解，提供沉浸式翻译的 skill，要参考 chrome 插件沉浸式翻译，提供最好的用户体验"
    },
    {
      "id": "evo_v20_001",
      "type": "coding",
      "title": "McpClient 配置增强：VSCode settings 可配置 MCP 启动参数 + 完整工具 Schema 存储",
      "status": "done",
      "depends_on": [
        "evo_v19_005"
      ],
      "acceptance": "package.json 中声明 browserAgent.mcp.* 配置项（browserUrl / autoConnect / headless / slim / extraArgs / noUsageStatistics）；McpClient.connect() 读取 VSCode settings 动态构建 npx 启动参数列表；listTools() 返回包含 inputSchema 的完整工具信息（不再仅存 name+description）；_discoveredTools 类型扩展为含 inputSchema 的接口",
      "acceptance_cmd": "cd packages/vscode-ext && grep -q 'browserAgent.mcp' package.json && grep -q 'browserUrl\\|autoConnect\\|headless' src/mcp-client.ts && grep -q 'inputSchema' src/mcp-client.ts && npm run compile 2>&1 | tail -1 | grep -v error && echo PASS || echo FAIL",
      "evolution_goal": "内置 MCP chrome-devtools-mcp；不考虑时间，使用最优解，提供用户使用 agent 的体验；结合 mcp 的工具再内置 skill"
    },
    {
      "id": "evo_v20_002",
      "type": "coding",
      "title": "McpClient 子进程健壮性：崩溃自动重启 + 健康检查 + 连接状态生命周期管理",
      "status": "done",
      "depends_on": [
        "evo_v20_001"
      ],
      "acceptance": "McpClient 实现 restartOnCrash 逻辑（transport close 事件监听 → 自动 reconnect，最多 3 次指数退避重试）；周期性健康检查（每 60s 调用 listTools 验证子进程存活）；dispose() 清理所有定时器和监听器；日志输出完整的生命周期事件",
      "acceptance_cmd": "cd packages/vscode-ext && grep -q 'restartOnCrash\\|autoRestart\\|reconnect' src/mcp-client.ts && grep -q 'healthCheck\\|health_check\\|HEALTH' src/mcp-client.ts && grep -q 'MAX_RESTART\\|maxRestart\\|retryCount' src/mcp-client.ts && npm run compile 2>&1 | tail -1 | grep -v error && echo PASS || echo FAIL",
      "evolution_goal": "内置 MCP chrome-devtools-mcp；不考虑时间，使用最优解，提供用户使用 agent 的体验；结合 mcp 的工具再内置 skill"
    },
    {
      "id": "evo_v20_003",
      "type": "coding",
      "title": "AgentLoop 工具 Schema 增强：将 MCP 工具的完整 inputSchema 注入 LLM 系统提示词",
      "status": "done",
      "depends_on": [
        "evo_v20_001"
      ],
      "acceptance": "agent-loop.ts 的 getToolDescriptions() 对 MCP 工具渲染完整参数签名（参数名、类型、是否必填、描述）；LLM 系统提示词中能看到如 'navigate_page(url: string [必填]) — 导航到指定 URL' 格式的工具描述；非 MCP 工具（browser_*）保持现有格式不受影响",
      "acceptance_cmd": "cd packages/vscode-ext && grep -q 'inputSchema\\|input_schema\\|parameterSchema' src/agent-loop.ts && grep -q 'required\\|必填' src/agent-loop.ts && npm run compile 2>&1 | tail -1 | grep -v error && echo PASS || echo FAIL",
      "evolution_goal": "内置 MCP chrome-devtools-mcp；不考虑时间，使用最优解，提供用户使用 agent 的体验；结合 mcp 的工具再内置 skill"
    },
    {
      "id": "evo_v20_004",
      "type": "coding",
      "title": "5 个 DevTools MCP 专属预设 Skill：性能审计 / 网络分析 / Lighthouse / 多页面工作流 / DOM 快照",
      "status": "done",
      "depends_on": [
        "evo_v20_001",
        "evo_v20_003"
      ],
      "acceptance": "skill-registry.ts 新增 5 个 preset Skill：devtools_performance_audit（start_trace → stop_trace → analyze_insight）、devtools_network_analysis（list_network_requests → get_network_request）、devtools_lighthouse_audit（lighthouse_audit → take_screenshot）、devtools_multi_page_workflow（new_page → navigate_page → evaluate_script → close_page）、devtools_dom_snapshot（take_snapshot → take_screenshot）；skill-runner.ts 工具路由支持 MCP 工具名（非 browser_* 前缀直接走 McpClient）",
      "acceptance_cmd": "cd packages/vscode-ext && grep -q 'devtools_performance_audit' src/skill-registry.ts && grep -q 'devtools_network_analysis' src/skill-registry.ts && grep -q 'devtools_lighthouse_audit' src/skill-registry.ts && grep -q 'devtools_multi_page_workflow' src/skill-registry.ts && grep -q 'devtools_dom_snapshot' src/skill-registry.ts && grep -q 'mcpClient\\|mcp_client\\|McpClient' src/skill-runner.ts && npm run compile 2>&1 | tail -1 | grep -v error && echo PASS || echo FAIL",
      "evolution_goal": "内置 MCP chrome-devtools-mcp；不考虑时间，使用最优解，提供用户使用 agent 的体验；结合 mcp 的工具再内置 skill"
    },
    {
      "id": "evo_v20_005",
      "type": "validation",
      "title": "DevTools MCP 深度集成全量验收：配置 + Schema + 健壮性 + 5 个 Skill + 双端构建",
      "status": "done",
      "depends_on": [
        "evo_v20_001",
        "evo_v20_002",
        "evo_v20_003",
        "evo_v20_004"
      ],
      "acceptance": "VSCode 插件编译通过 + Chrome 插件构建通过；package.json 有 browserAgent.mcp.* 配置声明；McpClient 支持可配置启动参数 + 崩溃自动重启；AgentLoop 系统提示词含完整 MCP 工具参数签名；5 个 DevTools MCP Skill 均已注册且可被 run_skill 调用",
      "acceptance_cmd": "cd packages/vscode-ext && npm run compile 2>&1 | tail -1 | grep -v error && cd ../../packages/chrome-ext && npm run build 2>&1 | tail -1 && cd ../../packages/vscode-ext && grep -q 'browserAgent.mcp' package.json && grep -q 'inputSchema' src/mcp-client.ts && grep -q 'restartOnCrash\\|autoRestart\\|reconnect' src/mcp-client.ts && grep -q 'inputSchema\\|parameterSchema' src/agent-loop.ts && grep -c 'devtools_' src/skill-registry.ts | xargs -I{} test {} -ge 5 && echo PASS || echo FAIL",
      "evolution_goal": "内置 MCP chrome-devtools-mcp；不考虑时间，使用最优解，提供用户使用 agent 的体验；结合 mcp 的工具再内置 skill"
    },
    {
      "id": "evo_v21_001",
      "type": "coding",
      "title": "修复 immersive_translate 及所有预设 Skill 的 argsTemplate 参数名不匹配问题",
      "status": "pending",
      "depends_on": ["evo_v20_005"],
      "acceptance": "skill-registry.ts 中 immersive_translate 步骤 2 的 argsTemplate 使用 texts 而非 paragraphs；审计全部 20 个预设 Skill 的 argsTemplate，确保每个步骤的参数名与目标工具的实际参数名一致；编译通过",
      "acceptance_cmd": "cd packages/vscode-ext && grep -A5 'llm_translate' src/skill-registry.ts | grep -q 'texts' && ! grep -A5 'llm_translate' src/skill-registry.ts | grep -q 'paragraphs.*\\$prev' && npm run compile 2>&1 | tail -1 | grep -v error && echo PASS || echo FAIL",
      "evolution_goal": "结合 debug-log 日志文件，不考虑时间，使用最优解，优化 Skill 执行体验"
    },
    {
      "id": "evo_v21_002",
      "type": "coding",
      "title": "增强 llm_translate 参数解析健壮性：多参数名兼容 + 结构化结果自动提取",
      "status": "pending",
      "depends_on": ["evo_v21_001"],
      "acceptance": "llm-tools.ts 中 handleLlmTranslate 支持从 args.texts / args.paragraphs / args.input 多个 key 中提取文本数组；当值为 JSON 对象字符串时自动从 .paragraphs[].text 或 .texts 或顶层数组提取；新增单元测试用例覆盖至少 5 种输入格式；编译通过",
      "acceptance_cmd": "cd packages/vscode-ext && grep -q 'args.paragraphs\\|args.input' src/llm-tools.ts && grep -c 'paragraphs\\|fallback\\|alias' src/llm-tools.ts | xargs -I{} test {} -ge 3 && npm run compile 2>&1 | tail -1 | grep -v error && echo PASS || echo FAIL",
      "evolution_goal": "结合 debug-log 日志文件，不考虑时间，使用最优解，优化 Skill 执行体验"
    },
    {
      "id": "evo_v21_003",
      "type": "coding",
      "title": "SkillRunner 插值增强：支持 {{$prev.key}} 路径表达式从结构化 JSON 中提取字段",
      "status": "pending",
      "depends_on": ["evo_v21_001"],
      "acceptance": "skill-runner.ts 的 interpolateValue 支持路径表达式语法：{{$prev.key}} 提取 JSON 对象的 key 字段，{{$prev.arr[].field}} 提取数组中每项的 field 组成新数组并 JSON.stringify，{{$step_N.key}} 同理；原有 {{$prev}} / {{paramName}} 语法向后兼容不受影响；编译通过",
      "acceptance_cmd": "cd packages/vscode-ext && grep -q '\\$prev\\.' src/skill-runner.ts && grep -q 'parsePath\\|extractPath\\|resolvePath' src/skill-runner.ts && npm run compile 2>&1 | tail -1 | grep -v error && echo PASS || echo FAIL",
      "evolution_goal": "结合 debug-log 日志文件，不考虑时间，使用最优解，优化 Skill 执行体验"
    },
    {
      "id": "evo_v21_004",
      "type": "coding",
      "title": "修复 Chrome SkillPanel skill_list 重复请求：useEffect 依赖稳定化 + 请求去重",
      "status": "pending",
      "depends_on": ["evo_v20_005"],
      "acceptance": "SkillPanel.tsx 中 skill_list 请求逻辑使用 useRef 追踪请求状态或 sendMessage 引用稳定化，确保连接建立后只发送一次 skill_list 请求；Chrome 插件构建通过",
      "acceptance_cmd": "cd packages/chrome-ext && grep -q 'useRef\\|requestedRef\\|hasFetched' components/SkillPanel.tsx && npm run build 2>&1 | tail -1 && echo PASS || echo FAIL",
      "evolution_goal": "结合 debug-log 日志文件，不考虑时间，使用最优解，优化 Skill 执行体验"
    },
    {
      "id": "evo_v21_005",
      "type": "validation",
      "title": "Debug-log 优化全量验收：immersive_translate 参数修复 + llm_translate 健壮性 + 路径插值 + 去重 + 双端构建",
      "status": "pending",
      "depends_on": ["evo_v21_001", "evo_v21_002", "evo_v21_003", "evo_v21_004"],
      "acceptance": "VSCode 插件编译通过 + Chrome 插件构建通过；immersive_translate Skill 的 argsTemplate 参数名正确；llm_translate 支持多种输入格式；SkillRunner 支持路径表达式插值；SkillPanel 不重复请求 skill_list",
      "acceptance_cmd": "cd packages/vscode-ext && npm run compile 2>&1 | tail -1 | grep -v error && cd ../../packages/chrome-ext && npm run build 2>&1 | tail -1 && cd ../../packages/vscode-ext && grep -A5 'llm_translate' src/skill-registry.ts | grep -q 'texts' && grep -q 'args.paragraphs' src/llm-tools.ts && grep -q '\\$prev\\.' src/skill-runner.ts && cd ../../packages/chrome-ext && grep -q 'useRef\\|requestedRef' components/SkillPanel.tsx && echo PASS || echo FAIL",
      "evolution_goal": "结合 debug-log 日志文件，不考虑时间，使用最优解，优化 Skill 执行体验"
    }
  ]
}